# Component reference

Generated by `python -m methane.engineering docs`; edit component metadata, not this file.

These are illustrative models. Numerical checks and finite-state verification do not establish real-plant calibration.

## Solar

Model: `dispatch-lab/solar-sections/2`

One-hour mean power; 0° azimuth south, −90° east, +90° west

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|
| Section capacity (`capacity_kw`) | kWp | 333.333 | 0–10000 | Illustrative assumption; not calibrated |
| Panel tilt (`tilt`) | degrees | 30 | 0–90 | Illustrative assumption; not calibrated |
| Azimuth (south = 0) (`azimuth`) | degrees | 0 | -180–180 | Illustrative assumption; not calibrated |
| Uniform shading (`shade`) | fraction | 0 | 0–1 | Illustrative assumption; not calibrated |
| Soiling (`soiling`) | fraction | 0 | 0–1 | Illustrative assumption; not calibrated |
| Converter limit (`converter_kw`) | kW | 1000 | 0–30000 | Illustrative assumption; not calibrated |
| Converter efficiency (`efficiency`) | fraction | 1 | 0.5–1 | Illustrative assumption; not calibrated |
| Nominal operating cell temperature (`noct_c`) | °C | 45 | 20–80 | Illustrative assumption; not calibrated |

Inputs: reference-plane irradiance: W/m²; ambient: °C; UTC interval start; section design; reference geometry; scenario power adjustment

Outputs: DC output: kW; section outputs: kW; cell temperature: °C; explicit losses/clipping: kW

Assumptions:

- 30% isotropic diffuse sky; archived reference plane normalisation
- Uniform section shading; independent ideal MPPT
- Panel tiles are schematic; no string I–V or bypass-diode model
- Hourly mean radiation; geometry evaluated at interval midpoint

Execution: `solar_model.component_step(SolarInput(design_json, sample_json, UTC time, ArrayCapacity, SolarContext)) -> ComponentResult`

Planning: `Same pure converter transforms every saved issue independently; output is an exogenous hourly DC-bus power limit`

Verification:

- tests/test_solar.py: design replay and altered geometry, no source mutation
- tests/test_engineering.py: independent solar calculations, bounded immutable caching
- tests/test_boundaries.py: standalone inputs, invalid data and I/O dependency firewall

References: [source](https://open-meteo.com/en/docs)

## Weather

Model: `dispatch-lab/saved-forecast/1`

UTC one-hour intervals; provider issues and local display times retain offsets

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|

Inputs: current PV: kW; current ambient: °C; UTC decision start; saved issues and availability times; horizon: whole hours

Outputs: hourly predicted PV: kW; ambient: °C; issue/availability provenance; current forecast residual: kW

Assumptions:

- Forecast boundary has no future realised weather input
- Availability at or before decision time; latest initialized issue wins
- Archive radiation stamped t+1 is the mean for [t,t+1); temperature sampled at t
- Missing intervals/units/nonfinite samples raise IncompleteWeather; no synthetic substitution
- Reanalysis is a historical reference, not a site measurement

Execution: `SavedForecastProvider.horizon(ForecastRequest) -> detached horizon; provider JSON is frozen and network-free`

Planning: `Exogenous pv_kw/ambient_c sequences feed component planning; CO₂ delivery is a separate DeliverySchedule`

Verification:

- tests/test_boundaries.py: isolated forecast causality and missing inputs, pure solar conversion and dependency boundaries
- tests/test_methane.py: normalization, null radiation, forecast availability and DST

References: [source](https://open-meteo.com/en/docs/single-runs-api), [source](https://open-meteo.com/en/docs/historical-weather-api)

## Reference_Pv

Model: `dispatch-lab/reference-pv/1`

One-hour mean power; no subhour cloud transients

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|

Inputs: reference-plane hourly irradiance: W/m²; ambient: °C; nameplate: kW; loss/NOCT/temperature coefficient

Outputs: DC generation: kW

Assumptions:

- Cell temperature = ambient + (NOCT−20) × irradiance / 800
- DC = min(nameplate, max(0,nameplate × irradiance/1000 × (1−loss) × max(0,1+coefficient×(cell−25))))
- Saved stress may adjust generation explicitly after conversion

Execution: `pv.convert(Parameters, irradiance_wm2, ambient_c) -> float kW`

Planning: `Exogenous generation input`

Verification:

- tests/test_boundaries.py: independent known-answer baseline

References: Conservation-law fixture; assumptions above, no empirical calibration source.

## Reactor

Model: `dispatch-lab/lumped-reactor/1`

Thermal helper accepts positive durations; dispatch and commitments use one-hour intervals

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|
| Thermal capacity (`thermal_capacity_kwh_per_k`) | kWh/K | 0.3 | 1e-06–10000 | Illustrative assumption; not calibrated |
| Heat-loss coefficient (`heat_loss_kw_per_k`) | kW/K | 0.08 | 0–10000 | Illustrative assumption; not calibrated |
| Production minimum (`temperature_min_c`) | °C | 250 | -49–1499 | Illustrative assumption; not calibrated |
| Production maximum (`temperature_max_c`) | °C | 400 | -49–1499 | Illustrative assumption; not calibrated |
| Maximum heating (`heater_max_kw`) | kW | 60 | 0–100000 | Illustrative assumption; not calibrated |
| Maximum heat rejection (`cooling_max_kw`) | kW | 40 | 0–100000 | Illustrative assumption; not calibrated |
| Minimum production run (`minimum_run_hours`) | h | 4 | 1–240 | Illustrative assumption; not calibrated |
| Minimum methane production (`methane_min_kgph`) | kg/h | 3 | 1e-09–100000 | Illustrative assumption; not calibrated |
| Maximum methane production (`methane_max_kgph`) | kg/h | 10 | 1e-09–100000 | Illustrative assumption; not calibrated |
| Running auxiliary demand (`auxiliary_kw`) | kW | 2 | 0–100000 | Illustrative assumption; not calibrated |
| Production electricity (`methane_electric_kwh_per_kg`) | kWh/kg CH₄ | 1 | 0–10000 | Illustrative assumption; not calibrated |
| Cooling electricity / rejected heat (`cooling_electric_fraction`) | fraction | 0.1 | 0–100 | Illustrative assumption; not calibrated |

Inputs: initial temperature: °C; ambient: °C; heater/cooling: kW; methane in interval: kg; duration: h

Outputs: ending temperature: °C; reaction heat: kWh; heat loss: kWh; operating mode; commitment: h

Assumptions:

- C dT/dt = heater + reaction heat − UA(T−ambient) − cooling
- Constant inputs within each interval; ideal Sabatier stoichiometry
- H₂=2, CO₂=44, CH₄=16, H₂O=18 kg/kmol; reaction heat 165 MJ/kmol
- No pressure, kinetics, gas-quality or water-recycling model

Execution: `Reactor.execute(ReactorState, ThermalInput, requested_running) -> ComponentResult; thermal and discrete state together`

Planning: `Reactor.planning(ReactorState, ambient_c, durations, enforce_commitment) -> PlanningBlock; thermal/gas/electrical ports with units`

Verification:

- tests/test_components.py: standalone thermal/gas planning, commitment carry, cold-start rejection, forced trip
- tests/test_engineering.py: independent ODE, subdivision and TLC discrete-state conformance

References: [source](https://doi.org/10.1039/D5RE00337G)

## Battery

Model: `dispatch-lab/dc-battery/1`

Positive-duration constant-power intervals; plant scheduling remains hourly. State is at interval end.

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|
| Usable energy capacity (`battery_kwh`) | kWh | 800 | 0–1e+09 | Illustrative assumption; not calibrated |
| Power / energy ratio (`battery_c_rate`) | 1/h | 0.5 | 1e-09–1000 | Illustrative assumption; not calibrated |
| Round-trip efficiency (`roundtrip_efficiency`) | fraction | 0.9 | 1e-09–1 | Illustrative assumption; not calibrated |
| Initial state of charge (`initial_soc`) | fraction | 0 | 0–1 | Illustrative assumption; not calibrated |

Inputs: initial stored energy: kWh; DC-bus charge/discharge: kW; duration: h

Outputs: ending energy: kWh; charge/discharge loss: kWh; SOC: %; named physical audits

Assumptions:

- eta_charge = eta_discharge = sqrt(roundtrip_efficiency)
- E_end = E_begin + eta × charge × duration − discharge × duration / eta
- Power limit = usable capacity × C-rate; charge and discharge are mutually exclusive
- Constant interval power; no self-discharge, temperature, degradation or SOC-dependent efficiency
- Zero capacity represents an absent battery; its displayed SOC is zero by convention
- Execution accounts for feasible applied actions; plant dispatch owns clipping, bus allocation and curtailment
- Usage-related wear is an economic allowance, not a physical capacity-fade model

Execution: `Battery.step(BatteryState(energy_kwh), BatteryInput(charge_kw, discharge_kw, duration_hours)) -> ComponentResult`

Planning: `Battery.planning(BatteryState, tuple[duration_hours, ...]) -> PlanningBlock(bounds, rows); local ports: energy, charge, discharge, charging (binary)`

Verification:

- tests/test_battery.py: hand-calculated round trip, Decimal reference, subdivision, empty/full/absent battery, invalid actions
- tests/test_battery.py: independent standalone MILP consumer, plan/execution conformance, exclusive directions
- tests/test_battery.py: implementation swap across all controllers, what-if, fallback, archive audit and display lineage
- tests/test_engineering.py::test_pre_refactor_physical_fixture: preserved physical trace

References: Conservation-law fixture; assumptions above, no empirical calibration source.

### Replaceable implementations

Both implementations expose `Battery.step(state, inputs)` and `Battery.planning(state, durations)`. Planning returns local energy/charge/discharge/direction bounds and linear rows; it has no SciPy dependency. Execution returns ending state, losses and audits. No implicit clipping is permitted.

- `affine/1`: Direct constant-efficiency state equation; unscaled energy-balance rows.
- `loss-ledger/1`: Bus/cell energy ledger; energy-balance rows scaled by one-way efficiency.

Select `Config(models=Models(battery="loss-ledger/1"))` or use advanced experiment setup. Defaults and archives without a selection use `affine/1`; legacy records are never assigned new provenance.

### Executed round-trip fixture

200 kWh capacity, 1/h C-rate, 64% round-trip efficiency. Start at 20 kWh, charge at 100 kW for 0.5 h, then discharge at 64 kW for 0.5 h. Independently: 50 kWh enters, 40 kWh is stored; 32 kWh returns to the bus. Losses are 10 + 8 = 18 kWh, ending at 20 kWh.

| Implementation | After charge kWh | After discharge kWh | Total loss kWh |
|---|---:|---:|---:|
| affine/1 | 60.000000 | 20.000000 | 18.000000 |
| loss-ledger/1 | 60.000000 | 20.000000 | 18.000000 |

The battery inspector's **Trace this result** links displayed energy, SOC and losses to recorded interval inputs, requested/applied actions, asset ID, model/implementation version and source hashes. Battery records and lineage have their own `/1` schemas inside additive methane schema 3 fields. [Integration and verification](battery.md).

## Electrolyser

Model: `dispatch-lab/electrolyser/1`

Positive-duration constant-power intervals; startup is an interval energy, bus demand is mean power.

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|
| Nameplate productive load (`capacity_kw`) | kW | 450 | 1e-06–100000 | Illustrative assumption; not calibrated |
| Minimum productive load (`minimum_fraction`) | fraction | 0.3 | 1e-09–1 | Illustrative assumption; not calibrated |
| Specific electricity consumption (`specific_energy_kwh_per_kg`) | kWh/kg H₂ | 55 | 1e-09–10000 | Illustrative assumption; not calibrated |
| Startup energy (`start_energy_kwh`) | kWh/start | 40 | 0–100000 | Illustrative assumption; not calibrated |

Inputs: previous on/off state; productive load: kW; available capacity: kW; duration: h

Outputs: ending on/off state; hydrogen: kg; water consumed: kg; total bus demand: kW; start count

Assumptions:

- H₂ = productive kW × duration / specific energy
- Water consumed = 9 kg/kg H₂; startup consumes energy but produces no hydrogen
- Constant efficiency and hard turndown; no stack temperature or pressure
- Available capacity is an explicit input: estimate during planning, physical capacity during execution; no injected fault labels

Execution: `Electrolyser.step(State(on), Inputs(productive_kw, available_kw, duration_hours)) -> ComponentResult`

Planning: `Electrolyser.planning(State, available_kw, durations) -> PlanningBlock; local power/electricity kW, hydrogen kg, on/start boolean`

Verification:

- tests/test_components.py: independent conversion/start calculations, standalone planning replay, implementation swap and failure limits

References: Conservation-law fixture; assumptions above, no empirical calibration source.

## Hydrogen

Model: `dispatch-lab/hydrogen-buffer/1`

Interval mass, not mass rate; state at interval end.

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|
| Hydrogen capacity (`capacity_kg`) | kg | 60 | 1e-09–1e+09 | Illustrative assumption; not calibrated |

Inputs: initial inventory: kg; interval inflow/outflow: kg

Outputs: ending inventory: kg; accepted inflow: kg; rejected inflow: kg

Assumptions:

- Uniform concurrent flow within the hour; inflow may feed production in the same interval
- No venting; infeasible overflow/exhaustion raises a named physical audit
- No pressure or leakage model

Execution: `Storage(Parameters(capacity_kg, 'hydrogen')).step(State, Inputs) -> ComponentResult`

Planning: `Storage.planning(State, intervals) -> PlanningBlock; inventory/inflow/outflow kg`

Verification:

- tests/test_components.py: independent ledgers, zero inventory, simultaneous flow, no silent venting, standalone planning and replacement

References: Conservation-law fixture; assumptions above, no empirical calibration source.

## Co2

Model: `dispatch-lab/co2-buffer/1`

Interval mass, not mass rate; state at interval end.

| Parameter | Unit | Default | Range | Source |
|---|---|---:|---|---|
| CO₂ capacity (`capacity_kg`) | kg | 1000 | 1e-09–1e+09 | Illustrative assumption; not calibrated |

Inputs: initial inventory: kg; interval inflow/outflow: kg

Outputs: ending inventory: kg; accepted inflow: kg; rejected inflow: kg

Assumptions:

- Delivery occurs at interval start before withdrawal; excess is explicitly rejected
- No pressure or leakage model; delivered feedstock is supplied CO₂

Execution: `Storage(Parameters(capacity_kg, 'co2')).step(State, Inputs) -> ComponentResult`

Planning: `Storage.planning(State, intervals, deliveries_kg) -> PlanningBlock; inventory/outflow/accepted/rejected kg, full boolean`

Verification:

- tests/test_components.py: full-tank delivery rejection before withdrawal, delivery accounting, isolated planning and execution

References: Conservation-law fixture; assumptions above, no empirical calibration source.

## Executed reactor example

Constant 60 kW heating from 20°C ambient, no production/cooling. This isolated fixture intentionally omits dispatch temperature limits.

| Hours | Temperature °C | Heat lost kWh |
|---:|---:|---:|
| 0.25 | 68.369761 | 0.489072 |
| 0.5 | 113.620011 | 1.913997 |
| 1 | 195.553746 | 7.333876 |
| 2 | 330.015335 | 26.995399 |
| 4 | 511.884660 | 92.434602 |

The full-plant scheduler remains hourly. Formal assumptions are in [the reactor specification](../formal/README.md).

Build artifacts contain executed plots, test counts from JUnit, measured timings and full TLC output; they are evidence for that specific build.
