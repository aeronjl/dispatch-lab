# Field operations programme

Authorized 11 September 2026: implement all six stages of [the roadmap](field-operations-roadmap.md), with reflection at evidence gates and user guidance for material changes in direction. The goal includes every hardware family and its prerequisite model, not just catalogue entries. No stage is complete on the strength of interfaces, illustrations or documentation alone.

**User pause, 12 September 2026:** finish only the bounded investigation-planning example's verification, then stop the programme. That checkpoint is now verified; see the final entry below and its [verification record](../build/services/investigation-alternatives/verification.md). Do not start another increment, resume the study queue or treat the unfinished stages as complete without a new user instruction.

**Subsequent bounded review, 12 September 2026:** the user authorized the [realism review](../research/field-realism-review/report.html), using the existing fixture and European sites. That review is complete; the [credibility gate](field-operations-realism.md) remains open for deployment/ROI and transferable-autonomy claims. It covers all 14 families, 31 assumptions, source evidence and conditional sensitivity checks. Structural capability and observation changes require guidance before further implementation. The following stage statuses remain implementation/numerical statuses, not established field realism. The wider programme and prior study queue remain stopped.

| Stage | Required outcome | Status |
|---|---|---|
| 1 | Replaceable service contracts, access, reservations, fractional events and compatibility | Implementation complete — executable integration and numerical gate checks; realism gate open |
| 2 | Cleaning coverage, bounded sensing, specific repair, assistance and support logistics | Implementation complete — bounded cleaning/sensing/repair mechanisms, finite support and visits, remote assistance, portable recovery and failed service hardware verified numerically; realism gate open |
| 3 | Full service economics, generalised Studies and published baseline comparisons | In progress — accounting, six baseline questions and their sensitivity interpretations are delivered, with original-source reference reproductions and reported causal-comparison limitations. Recovery, cleaning and information sensitivity bundles are verified. Remaining sensitivity preservation/reproduction work is still required; completed tiers must not be rerun merely because this former status summary was out of date |
| 4 | Service/production planning, recorded reasons and service what-ifs | In progress — bounded scheduling, charging, visits, conditional investigations, observed continuations, accepted operating tests, post-intervention beliefs and targeted alternatives are delivered, including original investigation-choice replanning. Repeated-computation, conditional-belief and investigation-choice recordings have verified original-source reproductions. Broader terminal/risk comparisons and complete service Model coverage remain required |
| 5 | All 14 hardware families, prerequisite mechanisms and comparative experiments | Pending |
| 6 | Degradation, predictive maintenance, reproducible learning and held-out evaluations | Pending |

Across every stage: preserve the quiet UI and original plant artwork; extend Model essays and recorded lineage; maintain explicit assumptions, causal information boundaries, resource/cost reconciliation, old archives and numerical reproduction. Record evidence and limitations, not an overall trust score.

Review points: after baseline service studies, after coordinated planning comparisons, after the hardware/design comparisons, and after held-out learning evaluation. Continue routine work autonomously. Ask for guidance when findings would materially alter agreed scope, required fidelity or the meaning of acceptance.

Initial baseline: `field-operations/1` is usable and remains available while its successor is developed. Its local policy and reduced physical mechanisms are documented in [field-operations.md](field-operations.md). Existing Studies publications retain their original scope.

## Work record

- Goal established. Implemented the execution-independent service contracts, registry, directed access graph, atomic stock/capacity ledger, fixed/mobile adapters and fractional-hour task executive in `methane/services/`.
- First executable proof: the same inspection request runs through fixed and mobile adapters, with independently reconciled 0.2 kWh bus use versus 0.8 kWh robot energy. It preserves delayed observations and separate acceptance. Results: `build/services/contracts/example.md` and `example.json`.
- 18 automated service-contract checks pass, including independent decimal arithmetic and generated capacity comparisons. `docs/service-contracts.md` documents interfaces and current limits. Stage 1 remains in progress: the generic runtime must still be integrated with the existing coupled plant and service configuration; legacy `field-operations/1` remains usable.
- Combined verification: all 37 service-contract, legacy field-operation, reproduction-bundle and evidence-catalogue checks passed. No existing physical model or main-scene illustration was changed in this foundation checkpoint.
- Next: build the versioned plant/effect adapter and existing-hardware definitions; reserve actual service power/isolation and adapt records to the established UI/accounting. Then complete section cleaning, specific repair/calibration, imperfect sensing and support logistics through those interfaces. No change in direction is indicated by the initial checks.

### Coupled integration checkpoint

- `plant-service-contracts/1` now assembles the core hardware through the generic registry and fixed/mobile adapters. Opt in through experiment setup; `service_system=null` retains the existing whole-hour runtime. Module replacement and flow calibration have separate effects, finite kits and acceptance conditions.
- Current work reserves actual utility rates, assets, work areas, travel corridors and conservative hourly isolation. The executive integrates fractional phase energy; plant execution receives applied service demand. Effects are committed only after the current plant interval, and post-work operating evidence must establish recovery.
- The shared dock is an explicit supply task and never credits future charging at dispatch. Original request evidence stays frozen when work waits for prerequisites; dispatch has a separate current context. Fixed readings can be erroneous or unavailable without exposing injected fault truth.
- New setup controls, an amber fixed-reader overlay and subhour animation preserve the original plant/solar SVG and reviewed legacy service scenes. Recorded inspection exposes mission/resource events, requested/applied electricity and cost assumptions. Detailed service Model essays and what-ifs remain later work.
- Coupled examples are in `build/services/coupled/report.md`, with mobile/fixed archives, offline playback, reproduction ZIPs and independent audits. The constant-power fixture verifies timing/accounting; it is not a field-economics study and must not be cited as annual hardware value.
- Verification at this checkpoint: 84 combined backend checks passed, plus nine Studies preservation tests. Browser checks: 17 passed, two optional tests skipped; original plant, solar and legacy-service screenshots passed unchanged. The new fixed-reader screenshot was visually reviewed. A protocol revision 4 explicitly freezes the new unused service defaults; published study editions and revisions 2/3 are unchanged.
- Next stage: section optical cleaning/coverage and partial work; sensor drift/dropout and independent evidence; retrieval, replenishment, remote assistance, visit bundling and failed-support recovery. Then the complete service-cost ledger and generalised published baseline studies. No findings require a change of direction or user guidance yet. The six-stage goal is still active.

- Final backend gate: the entire Python suite passed (218 tests, `build/services/coupled/full-pytest.xml`). Separate dependency-free restored-bundle checks passed: 2,051 checks for mobile and 2,003 for fixed inspection, with source/integrity checks and no numerical optimisation rerun. The local app was restarted on port 7860 with its original selected archive.
- Final renderer verification after the stranded-travel position correction: all seven service browser checks and 16 Node checks passed. Source-bound gate metadata and artifact hashes are saved in `build/services/coupled/verification.json`.

### Section optical cleaning checkpoint

- Added `section-optical/1` as an explicit opt-in mechanism. `array-surface/1` keeps piecewise covered area, removable/adhered losses and permanent damage; dry brushing changes only the treated removable portion. Surface transmission enters the existing section conversion before clipping. Legacy post-conversion runs retain their recorded meanings.
- Missions now integrate continuous consumable rates and partial-effect receipts. Brush condition changes between passes; actual area, work energy and materials remain consumed after interruption. Wind/rain and access prerequisites use declared assumptions or actual saved channels; missing conditions block work. The ideal surface-monitor assumption is explicit.
- Original plant and solar illustrations remain intact. Added recorded surface marks and section sweep paths, partial-failure poses and solar detail results. Static design previews remain distinct from executed cleaning. Restoring a design now moves focus away from the action bar it hides, preserving Escape navigation.
- The independent checker reconstructs optical conversion, coverage, brush efficacy, effect timing and stocks from recorded operands. Forecast error is compared on the same surface/converter basis; source-reference error remains separate. Future realised weather cannot change earlier cleaning or dispatch decisions.
- Reproducible mechanism examples, offline playback, saved archives and bundles: `build/services/cleaning/report.md`. At the reference constant-irradiance fixture, full brushing treats 5,000 m² and recovers electricity but adds no methane because the plant already reaches its production limit. The interrupted case retains 500 m² of cleaning and one stranded work order. This is not annual hardware evidence and does not change the programme direction.
- Verification: full Python suite 237 passed; final preservation/learning/surface checks 42 passed; all 40 Node checks passed. Full browser suite 40 passed with five optional tests skipped; four of those archive/offline checks subsequently passed using the new optical archive. The separate active-batch workload was not repeated at this checkpoint. Existing artwork snapshots match; new surface and interrupted-cleaner screenshots were visually reviewed.
- All four restored bundles passed offline integrity and independent calculations: 1,405 checks without cleaning, 1,520 with dry brushing, 1,436 for interrupted work and 1,520 with converter clipping. Source identities, test scopes and artifact hashes are saved in `build/services/cleaning/verification.json`.
- Reviewed the existing Model narratives against this mechanism, explicitly distinguishing the uniform-soiling teaching fixture from section-service runs; regenerated bound explanations and saved learning outputs. Full service essays are still outstanding. [Section-cleaning mechanics and limitations](section-cleaning.md) document this checkpoint.
- Stage 2 remains in progress. Next: explicit stranded-asset retrieval, replenishment/brush replacement, finite support/crew/remote availability and support failures; then richer sensing and portable/wet cleaning. Stages 3–6 remain required in full. The goal remains active; no material finding requires user guidance yet.

### Finite support and verified retrieval checkpoint

- Added opt-in `logistics/1` in `plant-service-contracts/3`. A crew retrieves a reported stranded robot using declared response, travel, preparation, towing, unloading and return phases. The location receipt does not establish health: a later supervised drive-test observation is required before new work. Failed tests and unavailable crew/communications/remote time remain explicit; the original failed mission, partial cleaning and spent resources are preserved.
- Crew shifts include the return journey, and actual crew/remote duration consumes finite period allowances. Typed upstream supplies, on-site capacities, accepted/rejected deliveries and brush replacement use the common stock ledger. Replacing a brush discards its remaining old allowance rather than generating negative wear. Remote time, installed brushes and rejected supplies are explicitly unpriced until the complete stage-3 cost model is delivered.
- The new independent reference reconstructs support chronology, lead/shift constraints, labour use, allowance boundaries, supply transfers, return flags and drive-test eligibility. Mutation checks reject early retrieval, forged return flags, backdated observations, missing passing evidence and altered labour counters. It verifies internal consistency of declared assumptions; it does not independently establish real robot reliability.
- The original equipment artwork remains unchanged. The service vehicle now stops to load the stranded robot, carries its existing sprite on a separate flatbed and unloads at the dock. Preparation, loading, transport and drive-test images were visually reviewed. A renderer sequencing bug was corrected: resumed work that started later owns the pose even when its queued request was created before the drive test.
- Saved examples and full source/input reproduction bundles: `build/services/support/report.md`. Offline integrity and independent calculations passed for retrieval (1,850 checks), failed drive test (1,830), unavailable crew (1,728), finite supply (1,936) and brush replacement (1,895). The retrieval example intentionally suffers another abort; its ending backlog is retained. No annual economics or hardware ranking is inferred.
- Verification: 254 Python tests passed; 44 Node tests passed. Fourteen service/optical browser checks passed, with the final transport-spacing/pose revision rechecked in seven of those tests. A further 28 platform/Model/Studies browser checks passed, with five optional active-batch/new-archive tests skipped in that invocation. Original plant, solar and legacy-service screenshot references matched unchanged. Detailed evidence scopes and source hashes are saved in `build/services/support/verification.json`.
- Reviewed all affected Model narrative bindings, kept the teaching examples distinct from service runs, and regenerated explanations/examples. [Support mechanics and limits](service-support.md) document the implemented scope. Stage 2 remains in progress: next are richer sensor evidence, portable/wet cleaning, visit bundling, remote assistance and explicit support-equipment failures. Stages 3–6 remain required. No finding at this mechanism checkpoint requires a change of direction or user guidance.
- All four optional new-archive/portable-report browser checks subsequently passed. Their first run exposed a robot shadow intercepting the solar-array click; decorative shadows and hidden decoration now ignore pointer input, while hardware remains selectable. The separate active-batch workload was not repeated here. Updated support playback and bundles include that correction; earlier source-bound archives are retained.

### Portable wet/dry cleaning checkpoint

- Added opt-in contracted portable cleaning through the same service contracts. `plant-service-contracts/4` and `array-surface/2` distinguish wet removal of adhered material from dry brushing; neither changes permanent damage. Each full-section pass reserves its operator, work area, setup and return, tool electricity, material kit and (for wet work) rinse/treatment water. Water supplies remain in litres, including accepted and rejected delivery quantities.
- Partial failure retains treatment, water and labour already consumed, and blocks reuse of the stranded operator/tool. Missing water never silently changes wet work into dry work. A store too small for a full section leaves that work blocked rather than repeatedly ordering ineffective deliveries. Compatible portable-tool assistance and visit bundling remain unfinished support work.
- Added a separate service vehicle/tool/operator/hose/brush overlay. Travel and brush position follow recorded phases; water marks appear only during active wet treatment. Wet, dry and interrupted poses were visually reviewed. The original plant and solar artwork and labels remain unchanged.
- Five saved 18-hour mechanism examples are in `build/services/portable/report.md`: no cleaning, dry cleaning, wet cleaning, unavailable water and interrupted work. Each retains ending inventories and unresolved work. Independent restored-bundle checks passed respectively: 1,657, 1,845, 1,902, 1,801 and 1,824 checks. Full wet treatment consumes 843.33 L on the reference section; the interrupted example retains 500 m² and 260 L. Available electricity improves without adding methane in this downstream-limited fixture. No annual ranking or empirical efficacy claim follows.
- Verification: 267 Python tests and 46 Node tests passed. All 16 service browser tests passed. A further 28 platform/Model/Studies browser tests passed with five optional archive/active-batch checks skipped in that invocation. Existing plant, solar, Model and Studies screenshot references matched. The exact source present before the backend suite is recorded in `build/services/portable/pytest-source.json`; checkpoint scopes and artifacts are recorded alongside the examples.
- Timing remains an explicit open item: the latest baseline browser samples measured 236 ms p95 for Model input-to-result and 859.5 ms for solar previews, with 1.1 ms and 3.5 ms p95 rendering respectively. The stricter 200 ms end-to-end aspiration was not met in those samples; the separate active-batch workload was not repeated here.
- Existing labour, visits and material kits are priced once. Portable hire, water procurement and rejected water remain explicitly unpriced until stage 3. Stage 2 and all stages 3–6 remain required; detailed service Model essays and comparative Studies publications are still outstanding. This checkpoint does not change the agreed direction or require user guidance.
- All four additional new-archive/offline browser checks passed against the wet-cleaning archive, explicitly requiring `array-surface/2` lineage. Original explanations, recorded component/cost calculations, offline playback and the saved Model report are accessible. Final scoped evidence is in `build/services/portable/verification.json`.

### Referenced inspection checkpoint

- Added opt-in `referenced-contact/1` in `plant-service-contracts/5`. Fixed and rover acquisitions measure a prepared 24 V contact plus internal zero/span references. Named noise, persistent reader offset/drift/dropout, shared stuck-contact faults, publication delay and evidence age are explicit. The rover assumes a compatible passive docking probe; it is not a camera that reveals hidden module health.
- The observation-only local rule can exclude a reader with a failed reference and use the other channel. Valid conflicting contact readings or a voltage between bands remain uncertain. Both readers can agree incorrectly when they share a stuck contact. Post-reset informative process tracking establishes recovery or supports escalation; contact agreement and completed work do not restore the process estimate.
- Known reset/module-substitution procedures invalidate earlier contact samples at their next eligible decision boundary, including packets delivered later. Acquisition quality and failed reference evidence remain recorded separately. Independent checks reject forged signals, classifications, publication times, displayed values, reader selection, procedure cutoffs and use of historical samples as current evidence.
- Private physical signal/error operands live only in retrospective records. The service supervisor receives eligible measurements and declared thresholds. Future fault changes cannot alter earlier decisions. Reader/contact faults persist through unrelated plant resets or module replacement; compatible reader service is later hardware work.
- Eight saved 32-hour mechanism examples are in `build/services/inspection/report.md`: normal, trip, reader drift, reader dropout, shared stuck contact, changed contact, delayed evidence and unavailable communications. Each retains ending plant/service inventory and unresolved work. All restored standalone checks passed: 2,703; 6,327; 6,327; 6,327; 6,333; 6,332; 4,746; and 2,710 respectively. These are workflow examples, not published hardware-value studies or empirical reliability evidence.
- Final verification: 292 Python tests passed, including 25 referenced-inspection tests. All 48 Node checks passed before the final evidence-only refinement; its two inspection renderer checks also passed afterward. Three inspection browser checks and four new-archive/offline checks passed on the final build. Earlier platform checks recorded two timeouts; both passed unchanged on retry. Initial service-browser failures and their corrected checks remain saved, rather than being erased from the evidence record.
- The original plant and solar screenshot references remain unchanged. The fixed-reader/rover scene and revealed evidence table were visually reviewed. Keyboard return, narrow-screen access, numeric fault controls, recorded calculations and offline original explanations are accessible. Preparation-port and reference hardware remain explicitly unpriced pending stage 3; actual reader/rover energy and usage stay in the existing ledger.
- Reviewed affected narrative bindings and regenerated teaching fixtures. Source-matched teaching checks are available; numerical verification remains distinct from empirical validation. Repository lint/format checks pass after tidying two research generators; temporary regeneration confirmed their report, catalogue and source records are byte-for-byte unchanged, and the original dated research verification was preserved.
- Performance remains open: the prior baseline samples measured Model input-to-result p95 572 ms and solar preview p95 345.2 ms, with rendering p95 1.5 ms and 4.3 ms. The separate active-batch workload was not repeated. Checkpoint source identities, artifact hashes, attempt history and limitations are in `build/services/inspection/verification.json`.
- Next: real multi-stop crew visits, finite remote assistance, compatible portable-tool/operator recovery, and failed docks/disabled service hardware. Bundling must change the itinerary and ledger, not merely apply a cost discount. Stages 3–6, deeper service Model workflows and all 14 hardware families remain required. No result at this checkpoint materially changes the agreed direction; the goal stays active.

### Combined crew visit checkpoint

- Added opt-in `crew-visit/1` under `plant-service-contracts/6`. A shared itinerary retains each original job, evidence, materials, effect and acceptance test. The crew travels out once, transfers between declared service points and returns after the last job. Crew and accompanying tools are held throughout; all current stocks and work areas are reserved atomically before departure. Same-visit future deliveries are not credited prematurely.
- The local rule combines already-known, currently feasible queued requests. A diagnosis discovered after an earlier repair becomes a later request. Work must fit a complete crew shift and return commitments; a short shift splits deliveries. No route optimality, vehicle cargo capacity or real travel-time calibration is claimed.
- Each completed effect stays distinct from acceptance. An unsuccessful replacement can remain unverified after the crew returns. Interruption of the current or an upcoming job stops the physical crew at its current location, blocks unstarted members, releases unused reservations and retains spent labour, water, material and partial treatment. Compatible portable-tool/operator recovery remains subsequent work.
- The existing vehicle, technician and portable-tool illustrations follow the active fractional-hour visit member. The vehicle follows declared transfers, and later cancelled jobs cannot hide a stranded crew. The revealed service inspector distinguishes planned jobs and return time from actual return and each job's acceptance. Transfer, module-work, tool-transfer, stranded and inspector images were visually reviewed. Original main plant and solar artwork and labels remain unchanged.
- Eight reproducible 32-hour workflow examples and full source/input bundles are in `build/services/visits/report.md`. Three supplies consume one visit and 3.5 committed crew-hours versus three visits and 7.5 hours under separate delivery; methane remains 260 kg in this downstream-limited fixture. Failed replacement, partial portable work, short shift and exhausted supply cases retain unfinished work and all ending inventories. These are mechanism examples, not the stage-3 published service Studies or annual economics.
- Full backend verification passed: 317 Python tests, including 25 new visit checks and independent decimal expectations. All 51 Node checks passed with the final renderer source; the three visit checks also passed after final fixture regeneration. Fifteen service browser checks passed, followed by 35 platform/Model/Studies/optical/fractional checks (five optional checks skipped), then all seven final visit/new-archive/offline checks. Existing plant, solar, Model and Studies screenshot references matched unchanged. Lint and format checks pass.
- All eight restored bundles passed integrity and standard-library numerical checks: supplies 3,205; separate supplies 2,591; interventions 3,016; unsuccessful interventions 2,972; portable transfer 4,107; interrupted portable work 3,813; short shift 3,100; empty pipeline 2,553. The checker reconstructs itinerary continuity, configured durations, atomic reservations, execution order, callouts, interruptions, display summaries and actual return. It does not establish empirical hardware reliability.
- Reviewed current narratives, regenerated bound explanations/examples and collected source-matched teaching verification. The existing price basis charges one departure callout plus individual hands-on/material charges; full committed crew hours are a resource quantity, not a duplicate travel-labour charge. Complete service cost allocation and procurement remain stage 3.
- Latest baseline browser samples measured Model input-to-result p95 82 ms and solar preview p95 372.6 ms, with rendering p95 0.4 ms and 4.2 ms. The solar 200 ms aspiration remains unmet; the separate active-batch workload was not repeated here. Source identities, archives and scoped evidence are in `build/services/visits/verification.json`.
- Next: finite remote assistance beyond the existing drive test, compatible recovery of stranded portable tools/operators, and failed docks/disabled service hardware. Recoverable control holds must remain distinct from persistent mechanical damage; completing a procedure must not reveal hidden success or certify recovery. Stages 3–6, all 14 families and deeper service Model/what-if workflows remain required. This checkpoint does not materially change the agreed direction. The goal remains active.

### Service-hardware recovery checkpoint

- `service-hardware/1` and `plant-service-contracts/7` add persistent mobile control holds, drive-power loss, failed charger power and portable pump-power loss. Remote release, prepared function tests, guided return, retrieval, typed module replacement and packing use finite power, labour, shifts, communications and spares. Observers see next-boundary command/tracking evidence, never injected cause or private procedure success.
- A successful test may permit a guided return. Failed remote release remains unverified even after a later replacement succeeds: acceptance is linked to the procedure's own test. Faults injected later cannot be cleared by earlier work. Interruption of guided return changes the retrieval origin and the eventual return links all stopped orders for that hardware. Packing moves the portable tool with its already-present uninjured operator; it does not repair or certify it.
- Ten 36-hour saved cases in `build/services/hardware/report.md` retain failed attempts, absent resources and ending inventories. All ten restored bundles pass independent checks (3,764–4,429 checks each), including no remote budget, absent spares, unsuccessful replacement and interrupted return. These are workflow fixtures, not the stage-3 published comparative Studies or empirical hardware claims.
- Full verification: 344 Python tests, 54 Node tests, 22 service browser checks and 31 platform/Model/Studies checks pass (five optional checks skipped in the main invocation). Both additional live archive checks pass; two offline checks pass after correcting their invocation's relative file URL to an absolute path. The original plant, solar, Model and Studies screenshot references remain unchanged. All eight new hardware images were reviewed. Current documentation and teaching evidence match the tested source. Details, attempt history, source stamp and artifact hashes are in `build/services/hardware/verification.json`.
- Stage 2's mechanism gate is complete: useful and failed recovery, inconclusive/shared-fault observations, blocked access, exhausted supplies, stranded recovery, explicit finite assistance, visit logistics and hardware failures are covered by this and the optical/portable/inspection/support/visit checkpoints. This does not complete service-specific essays, full economics, generalised Studies, planning, all 14 hardware families or learning. Those remain required across stages 3–6.
- Latest baseline Model p95 is 94 ms input-to-result and 0.5 ms rendering; solar is 626.3 ms and 2.2 ms respectively. The solar interaction aspiration and active-batch follow-up remain open. Next is stage 3: complete allocated/decision/expenditure accounting and matched, published field Studies. No mechanism finding requires a material direction change or user guidance.

### Stage-3 recorded economics adapter

- Added `methane/service_economics.py` with complete versioned illustrative assumptions and separate allocation, action-dependent and modelled-expenditure views. It reads finite logistics events, prices full crew time once, separates remote work, handles shared callouts and declared vehicle time, distinguishes procurement/consumption/rejection/opening stocks, and retains ending stores. Applicable missing prices leave totals undefined. Purchased versus contracted/shared provision has explicit coverage rules. Replaceable subsets are excluded from body ownership; calendar, usage and consumed parts are reconciled rather than added twice.
- `reprice_run` substitutes the old service subtotal once in a complete plant report without changing saved physical actions. Immutable HTML/JSON editions preserve their exact pricing source capsule separately from the source run. Three final examples are indexed in `build/services/economics/examples.json`. All three reproduce their full report dictionaries from restored pricing source with network connections blocked. This is explicit repricing evidence, not a dispatch-policy change or published field Study.
- Full suite: 361 Python tests pass. All 17 economic tests passed again after the final source-preservation addition. Independent decimal examples cover crew/remote work, parts, procurement, rejected water and contracts; execution cases verify immutable repricing. Three final reports pass offline figure checks, keyboard disclosure and narrow-screen access. Desktop and narrow images were visually reviewed. Single report generation in the 36-hour examples took approximately 36–39 ms; this is not a p95 interactive/batch performance claim. Source scopes, initial test/invocation corrections and final artifacts are in `build/services/economics/verification.json`.
- Stage 3 remains in progress. The adapter does not yet replace the live cost panel, implement scheduled maintenance visits/standby demand or generalise the Studies resolver. These are the next required steps, followed by the six baseline study questions. Stages 4–6, all 14 families, deep service essays and service what-ifs remain unchanged in scope. No result requires user guidance or a change of direction.

### Integrated service accounting checkpoint

- Complete service prices can now be frozen in a run and edited through the revealed setup controls. Allocation, decision costs and modelled expenditure appear in the service inspector; recorded calculation links carry both original dispatch and current report price identities into Model. Repricing preserves actions and original assumptions. Blank applicable prices remain explicitly unpriced, including after hiding or restoring controls. Contract/shared coverage prevents duplicate ownership and wear charges.
- Compact playback summaries keep detailed cost operands behind inspection. The independent standard-library reference recalculates all three service views and checks each decision's original service-price identity. New bundles include readable service reports and their pricing source; differently priced exports retain separate files without replacing the earlier report. Old archives and the battery study protocols remain available; revision 7 freezes the added inactive configuration default.
- The final 36-hour recovery example is `build/services/economics-integration/release/3b45d9ae9eb79bce.json.gz`. Every applied action and plant state matches the earlier mechanism fixture. Its restored bundle passes all 4,191 independent checks. This remains an illustrative workflow example, not an annual economics or hardware-ranking study.
- Full verification passed: 364 Python tests, 54 Node checks, three new service-accounting browser checks and 36 existing platform/Model/Studies/service checks. Four final new-archive/offline browser checks also pass. Existing plant, solar, Model, Studies and service screenshot references remain unchanged; new inspector and narrow-editor images were reviewed. A final offline price-revision omission was corrected after the full suite; all 52 relevant bundle/economics/lineage/learning tests passed afterward. Source identities and exact scopes are recorded in `build/services/economics-integration/verification.json`.
- Verification caught and corrected the new service-price digest, blank-price restoration and missing offline response metadata. Earlier failed attempts are retained. Current teaching evidence matches the checked source and documentation bindings are current. These findings reinforce keeping identities and unknown values intact across every adapter; they do not require a direction change.
- Latest baseline Model p95 is 84 ms input-to-result and 0.4 ms rendering. Solar preview is 339.9 ms and 1.3 ms respectively; the 200 ms solar aspiration and separate active-batch follow-up remain open. Stage 3 still requires actual scheduled-maintenance and standby-power mechanisms, a generalised Studies resolver, and the six matched service-study questions. Joint planning, the remaining hardware families, service essays/what-ifs and learned policies remain required. The six-stage goal stays active.

### Multi-arm field Studies and first reference publication

- Added versioned separate-arm field protocols, matched background/weather checks, complete frozen policies/configurations and all resolved changes. The old battery protocols and publications remain available. The revealed Studies workspace supports question selection, cancellation/resume, preserved publication history, original cost traces, terminal stores/backlog and exact-case playback return.
- Published the 48-case, 72-hour recovery reference (four packages × four conditions × three seeds): edition `fa16440b7a984f999dd9659c59ab55aa`, authored report `fce9ebd27fcc48b3b4ce2a5c07c81e72`. All 36 matched comparisons completed; all 446,380 scoped independent checks passed. The portable bundle passed its standard-library restoration checks. A separate reproduction, `c16556920ad448aeacd1616cddce3ab3`, matches every applied action and methane total across all 48 traces. [Protocol, report and preservation boundary](field-studies.md).
- Reflection: fixed sensing/reset handles the modelled latch with lower allocated cost than the rover package; permanent damage still requires human intervention. Failed procedures remain in the evidence. Calibration adds no methane where independent balance sensing already sustains dispatch. Physically restored capacity can remain unverified because the local controller does not schedule enough informative load tests. Terminal backlog, sensor redundancy and post-service evidence must therefore be explicit in stage 4. This supports the agreed plan and does not justify dropping any hardware family.
- The first implementation gate passed 375 Python checks, 54 Node checks, four field-Studies browser checks and 36 preservation checks (five optional checks skipped). Under an active comparative batch, Model measured 73.1 ms p95 input-to-result and 0.5 ms rendering. Solar's latest baseline is 438.5 ms/1.2 ms, so its 200 ms aspiration remains open. Full-size publication verification additionally exposed a stale-report navigation race and expensive archive serialization. Both are corrected: pending publication loads remove old actions, and archive/ZIP writes publish complete files atomically. The single profiled export sample fell from 20.06 s to 1.76 s; this is not a p95 claim. The last live replay sample took 5.24 s under concurrent verification work. Exact final scopes and source changes are retained in `build/services/field-studies/verification.json`.
- Stage 3 remains open: actual scheduled-maintenance execution and standby power, additional service policies and the five other independent question publications are required. The sensitivity tier has not run. Joint planning, remaining hardware mechanisms, complete service Model/what-if workflows and learned policies remain required in stages 4–6. The all-six-stage goal remains active; no user guidance is currently needed to continue.

- Final checkpoint verification: all 377 Python tests and 54 Node checks pass; all five field-Studies browser checks and 36 preservation checks pass (five optional cases remain skipped). The real 48-case write-up, original service-cost trace, failed-repair playback, originating-report return, keyboard exit and 390 px access also pass. The restored report and playback work with HTTP blocked. Original artwork references are unchanged. Desktop/narrow study screenshots were reviewed. Offline prose is currently flattened into a long paragraph by the Markdown report renderer; paragraph separation is a remaining readable-export correction, not a numerical failure.
- Final Model baseline measures 88 ms p95 input-to-result and 0.4 ms rendering; solar is 328.6 ms/1.3 ms and remains above its interaction aspiration. The earlier 73.1 ms active-batch Model result remains scoped to its recorded source. Teaching evidence now matches checked source `64cef0c24fa36638b66356ca556b8e81b930f63679151b0550e3c4bf275b2432`. Two new archive tests initially compared Python tuples with JSON lists; corrected contract comparisons pass, including interrupted/comcurrent writes. Prior attempts are retained.


### Executed routine work and dock standby checkpoint

- `plant-service-contracts/8` adds opt-in `routine-service/1` and `dock-standby/1`. Due work consumes finite crew time, journeys and typed supplies, can share visits, excludes conflicting target use and repeats from actual work completion. An interruption cannot earn completion credit. Routine work does not silently repair a fault, calibrate a sensor, renew a brush or extend equipment life. Standby draws real bus energy, can deplete plant storage, reports unmet demand and gates dock controls; a failed charger power stage may still consume control electricity. [Mechanics and assumptions](routine-services.md).
- Setup retains fractional hours and power. The revealed inspector shows due clocks, resources and original costs. Recorded routine visits use the existing vehicle and technician artwork; their dock parking was moved clear of the CO₂ vessel. Original plant and solar drawings and labels are unchanged. The new inactive protocol revision 2 preserves the original recovery-study boundary; older publications, their original prices and source remain intact.
- Three final examples are in `build/services/routine/checked/report.html`: routine work, a night reserve challenge and unavailable support. All 9,170 independent checks pass, including restored standard-library bundle checks. Restored-source numerical reruns match every applied action, methane total and allocated cost. These are mechanism examples, not comparative or annual maintenance-value studies. Their checked numerical/rendering source is `541a5bda460dba98a98c0f7122ee8b0f15dd5014302fad8dafbceea99018f8bf`.
- Reflection: the local controller supplied 2 kWh of night standby and left 10 kWh unserved after using plant storage for process operation. Constant-solar cases retain overdue work when finite supplies or crew prevent it. The absence of a degradation benefit is explicit: the fixed-fault model cannot justify a claim that routine maintenance avoids failures. These findings support the planned service-reserve work in stage 4 and degradation hypotheses in stage 6; no change of direction or user guidance is needed.
- Verification: 398 Python tests and 58 Node checks pass. Across the preserved browser run and focused correction, 56 distinct browser checks pass and five optional checks remain skipped. The new fractional-input assertion initially expected an explicit null where Gradio omits an unconstrained precision; the corrected test and all three new UI checks pass. Earlier attempts also exposed the old solar-only physical service bound and a missing independent duration entry for shared routine visits. All attempts and exact scopes are retained in `build/services/routine/checked/verification.json`; no failed publication was overwritten.
- Model explanations and teaching evidence match the checked source. Latest Model p95 is 84 ms input-to-result and 0.5 ms rendering; the archived 72-hour solar preview measures 387.5 ms and 1.2 ms, so its 200 ms aspiration remains open. The actual old recovery report now also has a separately published paragraph-preserving presentation (`97857d393a23450289152fd7075e01f3`); its numerical evidence and authored text are unchanged, and the older report and portable bundle remain available.
- Stage 3 remains in progress: deliver periodic/alternative service policies and the five other independent baseline question publications, including sensitivities and unsuccessful cases. Stages 4–6, every remaining hardware family, complete service Model essays/what-ifs and learned/held-out policies remain fully required. The all-six-stage goal stays active.

### Explicit service policies and prospective baseline questions

- Added `local-cleaning-policy/1` and `contact-access/1` under opt-in `plant-service-contracts/9`: no cleaning, current-condition cleaning or recurrence from full-pass completion; compatible prepared contacts versus known enclosed contacts. Partial work changes its actual surface and consumes resources without earning full-pass credit. The original rules and artwork remain available. [Mechanics and scope](local-service-policies.md).
- Added five complete prospective question protocols and question-specific Python outcome tables for cleaning, information, support, provision and inspection-interface design. Active solar geometry is preserved during explicit capacity/converter transforms. These are specifications until their published comparisons complete; inspection access does not complete the later replaceable-module/manipulator families. [Questions and boundaries](field-baseline-questions.md).
- Four saved mechanism examples at `build/services/local-policy/checked/examples.json` pass 13,130 independent checks. Their original source is `392bb5a1eceec2f0d7cb7a789014820145c05084235a0648e72fcf216fcb6618`. All four restored standard-library checks pass, and original-source numerical reruns match every action, methane total and allocated cost. The interrupted pass retains 500 m² of treatment without full-pass credit. Prepared-contact evidence becomes eligible at H6; an enclosed contact supplies none and selects qualified human intervention. These are mechanism examples, not hardware-value conclusions.
- The first cleaning edition `12a91febf8a44abcabca0ab53367f00f` is withdrawn, with report `198837044d514266aed4276e96074803` preserving its partial/failed attempts. Revision 1 omitted the contracted crew required by its portable tool. Revision 2 corrects the arm, and configuration validation now rejects this omission before execution. No failed publication or numerical trace was overwritten.
- A real portable smoke also exposed an independent-checker timing omission: current-supply interruption and packing may occur in that order at the same decision boundary. The checker now accepts only an interruption recorded no later than dispatch; a forged later interruption fails. It does not use later truth to justify a return.
- Final backend regression: 418 tests pass at source `d98bf755048bf9282d0388ad1ea4338377ca5662fecfadebcb6122508e58ff28`. Earlier complete regression was 416; all 58 Node checks and 21 browser checks pass, with two optional archive/browser cases skipped. Original plant/solar screenshots match; prepared/enclosed fittings and the actual 390 px inspection viewport were visually reviewed. Browser scope precedes the final validation/checker-only corrections; exact artifacts are retained under `build/services/local-policy/checked` and `attempts`.
- Stage 3 remains open: execute and interpret the corrected remaining reference studies and all required sensitivities, preserve unsuccessful/incomplete cases, and verify original-source reproduction. Stages 4–6, full service Model/what-if workflows and every hardware family remain required. No finding changes the programme direction or requires user guidance. The all-six-stage goal remains active.


### Five reference questions and versioned reporting correction

- The cleaning, information, support, provision and prepared-inspection references completed 186 cases and 120 matched comparisons, with 2,438,298 independent checks. All five reviewed write-ups are linked from `docs/field-baseline-questions.md`; the original numerical execution source remains `d98bf755…`. Negative and mixed outcomes are retained. Short references do not establish annual value, calibrated reliability or full autonomous recovery.
- Cleaning can recover electricity without methane when CO₂ is exhausted. Additional contact evidence selects a permitted remedy without necessarily shortening anomaly detection; a shared faulty contact remains ambiguous. Bundling can reduce visits but cannot create missing access, crew, references or stock. Charger repair costs more within this short initial-charge fixture. Provision comparisons distinguish acquisition, period allocation and marginal prices; equal resident availability is an explicit assumption. Prepared inspection access does not enable robotic module replacement.
- Reviewed record inspection found that the derived restoration count omitted the executive's `awaiting verification` state. `field-study-reporting/2` corrects 114 counts while preserving every other case outcome and original attempt. Its source capsule is saved separately from the original run source. New publications identify that derivation; missing original reporting code is an error rather than a reconstructed claim. The live app shows both the original and corrected report revisions.
- Source-bound checks pass: 432 Python tests, 58 Node checks, seven general Studies browser checks and five field-study checks. The first full attempt correctly failed two documentation freshness checks; the reviewed experiment explanation and teaching snapshots now bind the changed implementation. A live check found lost focus when report selection hid its originating button; focusing the persistent Back control before loading fixes Escape during and after selection, with a regression assertion. Reviewed desktop/narrow screenshots and exact report/case navigation are in `build/services/local-policy/checked/`. No original plant or solar illustration was edited.
- The original authored cleaning bundle (554,636,682 bytes) passed dependency-free inventory and independent physics/accounting checks for 48 archives. Its source and original publication remain intact. Revised exports, other question bundles, original-source numerical reproductions and all 702 sensitivity cases remain required; the upcoming queue does not count as completed evidence.
- The live source remains stable during its checks. Study workers explicitly execute their own preserved source directories and Python paths; future development must never modify those frozen roots. This allows distinct captured study editions to remain reproducible as the current application advances. The next-planner review is recorded in `build/services/planning/preflight.md`, including return/tool energy, operating tests, common-target uncertainty, terminal obligations and semantic identities for future mission random draws. Stages 4–6 and all 14 hardware families remain in scope.

### Accepted-event matching and continued reproduction checkpoint

- Added optional `target-action-request/1` in `plant-service-contracts/10`. Events are numbered on acceptance separately for each target/action; candidates and rejected dispatch do not consume a number, while cancelled accepted work retains one. Wording, actor, schedule, unrelated jobs and draw order cannot change the matched variate. Event identities are public; realised draws remain private execution evidence and are saved only retrospectively. Existing defaults and frozen studies retain `legacy-reason/1`.
- The independent checker reconstructs accepted ordinals and exact 53-bit fractions, then checks compatible core repair, hardware and supervised-test outcomes. Focused tests cover fixed independent vectors, invalid inputs, combined visits, partial failure, retrieval, probabilities, causal separation and forged ledgers. Current field protocol revisions explicitly freeze the compatibility setting and reject an arm-specific change to the matching model.
- Three saved mechanism examples passed 12,318 independent checks, offline bundle checks and original-source numerical reruns with identical actions, methane and costs. Their source is `492d03e8…`; see [outcome matching](service-outcome-matching.md) and `build/services/matching/restoration-checks.json`. These are verification examples, not a hardware ranking or a completed joint planner.
- The final release source `25151fd1700479382a512582b3ad8ffd67f85c6828800f00306ef4443f46354b` passed all 459 Python tests and 58 Node checks; formatting, lint and Model freshness pass. The matching control is revealed only for fractional service execution, with reviewed desktop/narrow layouts. Setup return preserves controller and hour. The original plant/solar screenshot checks still pass. Browser checks retain an archive-opening five-second timeout under concurrent load separately from the subsequent focused recheck; see the release evidence for its outcome rather than treating a timeout as a pass.
- Cleaning (48 cases), information (36) and support (42) now have successful original-source reproductions and revised portable exports. All 126 complete service records, retrospective states, physical actions and metrics match, with matching source/weather and passing independent checks. `build/services/local-policy/checked/reproduced-service-trace-review.json` contains the exact comparisons. The two remaining reference reruns and all six sensitivity editions continue in the frozen-source queue; stage 3 is still open.
- Reflection: stable event matching was a necessary prerequisite discovered from the baseline traces. It does not change the agreed direction or remove any of the 14 families. Next is candidate scheduling before live acceptance, explicit future utility/isolation trajectories, observation branches, recovery tests and terminal obligations. Existing local rules remain baselines; all stage-4 alternatives, stage-5 mechanisms/artwork/studies, service Model essays and stage-6 degradation/learning remain required.
- Final browser verification passed all six Studies/artwork checks. The archive-import observation completed in 5,153 ms with the original 5,000 ms assertion; a separate diagnostic confirmed successful playback and return. The functional test now checks immediate pending feedback, allows a bounded 15-second import and saves latency separately. Its final measured sample was 4,811.95 ms. Neither observation establishes instantaneous UX or a p95 result. The two original timeout traces/logs remain part of the evidence.
- The provision reproduction and revised export subsequently completed 36 cases. All four reviewed rerun questions now total 162 cases with identical service records, retrospective states, metrics, source and weather, and passing independent checks. Its rerun edition is `cabdcd5ef69b4451992e7987f05d5fd8`, report `106c9754b9a246b7b0f8bb914d126c4b`. The interface rerun/export and all sensitivities are still pending or running in the frozen queue.

### Stage-4 service decision and demand ports

- Added `service-demand-projection/1` and `service-process-demand-coupling/1`. Current observation/verification preparation is separate from mission acceptance. Repeated proposals do not reserve supplies or consume accepted-event ordinals. Selected work is rebuilt from registered recipes and rechecked against actual resources. Existing `begin` keeps local policy and visit semantics.
- Remaining missions expose hourly bus energy, concurrent power peaks, robot energy, stage-entry versus rate supplies, isolation and unfinished work/return obligations. The coupled feasibility calculation uses the original estimated state, identified eligible forecast and process prices; a future tool-power shortfall, missing publication timing, joint resource conflict or completion beyond the declared boundary is explicit. Lack of a validated solver incumbent remains unresolved, not proof of infeasibility.
- The active fixed-service solar-only restriction is preserved. Forecasts do not silently authorize battery-backed tools, future stock credits or restored equipment. This port does not yet model service benefit, price candidate service work, choose observation branches or claim a complete joint supervisor. See [service planning ports](service-planning-ports.md).
- Twenty-four focused checks pass, including independent decimal expectations, reconciliation with actual executive consumption, future forecast exclusion, current-resource staleness and mutually conflicting candidates. Six earlier workflows were executed from frozen source `c24c5830…` and current code; all 72 full service records and fault states match exactly.
- The source `9b27e4c0082b8a43e54b9c0dc8a9c46fd2485f679a07fd611031fc19664d9e37` passes 483 Python tests and 58 Node checks. Model narratives and examples were reviewed/regenerated with current bindings; catalogue, generated reference, documentation freshness, formatting and lint pass. All 28 browser asset files and the app entry point are byte-identical to the previous verified checkpoint.
- Three five-hour primitive-level examples preserve inputs, candidate schedules, physical actions, service ledgers and source under `build/services/planning/window-example`. A delayed inspection completes at 4.25 hours; the immediate task is interrupted by the forecast low-power interval. An optimistic forecast accepts immediate work but actual low power still interrupts it. These are timing/forecast-boundary examples, not hardware rankings or standard full-run archives. Restoring the 275-file source capsule and rerunning with network calls blocked reproduces all values except 15 solver-duration observations.
- The final interface rerun now matches all 24 original cases. Together, the five subsequent study reproductions cover 186 cases with identical service traces, retrospective states, metrics and original source/weather. All five reviewed portable exports pass standalone integrity and complete-archive checks. The six sensitivity editions remain in the frozen queue; recovery sensitivity was observed running at approximately 84% in this checkpoint. Progress is not a completed result, and stage 3 remains open.
- Reflection: current tool-power limits and service continuation obligations are now explicit inputs to a future joint decision. Forecast error still requires actual execution interruption and replanning. Next are task benefits and possible observation outcomes, service decision costs, joint selection/charging, recovery reserves and terminal assumptions, followed by the original-information UI alternatives and paired studies. All stage-5 hardware mechanisms/artwork/studies, service Model essays and stage-6 degradation/learning remain required. No finding requires a material direction change or user guidance; the goal remains active.
- Final browser verification passes both original plant/solar screenshot preservation and keyboard inspection checks. The app was restarted on its existing port and saved archive with the tested backend. Immutable artifact copies and scoped results are in `build/services/planning/port-checkpoint/verification.json`. The initial arithmetic-test fixture-construction errors are retained separately from the final passing suite.

### Costed outcome branches and bounded schedule selection

- Added `service-decision-pricing/1`, optional `dispatch-lab/service-economics/2`, `service-outcome-tree-mpc/1`, and `service-schedule-supervisor/1`; coupling is now version 2. Candidate prices use the same accounting arithmetic as recorded activity and subtract the cost of existing commitments. Fixed ownership stays outside dispatch incentives. Usage/parts pools are not charged twice; uncertain supply disposition, unfinished costs and missing prices remain explicit. Version 1 stays the default and twelve original 120-hour service cost reports match the previous source exactly.
- Scenario MPC enforces shared actions until a declared observation distinguishes branches, retains forecast publication/time boundaries, and supports explicit expected/worst-case risk and plant terminal minima. A small independently calculable two-hour fixture rejects the spurious 5 kg expected methane that separate clairvoyant branch solves would suggest. Capacity profiles are declared estimates, not restored fault truth.
- The bounded selector checks supplied registered recipes against current resources, explicit procedure deadlines, service-stock floors and the common scenario tree. It records every candidate, solver limit, unattempted alternative and unresolved result. Cancellation and stale-response checks leave work unaccepted. Procedure completion does not establish useful evidence or verified recovery. This is callable experimental machinery, not yet the default run-level supervisor; conditional remedies, actual recovery tests, charging, rescheduling, UI alternatives and paired planner studies remain required.
- Executed verification exposed a missing boundary: complete candidate prices must use the finite-logistics execution ledger expected by the resulting report. The explicit guard and regression test now enforce this. The initial example failure is retained. The corrected example selects hour 2, finishes at 4.25 h, consumes 0.45 kWh and reconciles its EUR 18 price. Optional inspection is deferred without invented information value; an early deadline remains infeasible. Its three comparisons and one five-hour execution are primitive examples, not complete controller-performance studies.
- The final source `24982a885b579a959deec26f6a317851694708cffc26a24429c4c883cda85d86` passes 516 Python checks. Final browser checks pass original plant/solar artwork, keyboard inspection and all twelve Model topics. All app/browser asset bytes are unchanged. The source capsule restores 282 files and reproduces the example offline: physical actions, quantities and costs match; measured solver/comparison durations and the correctly derived identity of those recorded rows differ. The initial overly strict trace-identity comparison is preserved. The earlier 515-test run and browser observations remain separately scoped.
- The Model narratives and saved examples were reviewed and regenerated; lint, formatting, catalogue and documentation freshness pass. The global evidence collector correctly leaves mutation, formal, wider browser/performance and full-run offline/recomputation gates as not run for this new source. This checkpoint does not claim those broader gates passed. Scoped, immutable artifacts are in `build/services/planning/choice-checkpoint/verification.json`.
- Recovery sensitivity completed 144 cases/108 pairs. Reviewed publication `191ba94cff3a43b389bc2ccf8b8d75b0` retains failed procedures and unverified recovery. All original independent-audit artifact hashes match. Inspection of case `02d7559064e9d75e3123de65` shows a physical reset by decision 14, but full-capacity probes at hours 84 and 107 are not consecutive; the controller remains unconfirmed at hour 119. The next supervisor needs to reserve actual informative tests and their energy/feedstock headroom, not a fixed confirmation delay. These findings support the programme rather than changing its direction.
- Cleaning sensitivity also completed 144 cases/108 pairs, with descriptive report `c371bd1b8d7e45d2ae0b8a10211862f4`; its interpretation remains to be reviewed. Information sensitivity is running, and support/provision/interface follow in the frozen-source queue. Sensitivity exports and required numerical reproductions remain open. Stages 3–6, full coverage of the 14 hardware families, service Model essays and learned/degradation experiments are not complete. The all-six-stage goal remains active.


### Scheduled recovery tests: source 8e617d4d

- Added opt-in `scheduled-load-tests/1` through `dispatch-lab/policy/2`. The scheduler reserves consecutive requested loads using the original estimated state, saved forecast, accepted service demands and declared tracking/unchanged-delivery hypotheses. It never receives injected fault identity, physical repair success or scheduled recovery. Shared requests are distinct from possible delivered power in `service-outcome-tree-mpc/2`. Actual execution and sensor evidence determine confirmation.
- Strict confirmation requires consecutive requests at the same load with consistent electrical tracking and an independent hydrogen balance. Skipped tests, changed loads and ambiguous evidence break the sequence. An already-isolated flow meter does not veto independent evidence. Eligible public procedure receipts can reopen an opportunity without asserting success. Failed tests, missed deadlines, retries, solver limitations and unresolved windows remain recorded. Defaults and version-1 policies retain their earlier semantics.
- Experiment setup reveals these assumptions only after enabling the experimental option. Greedy's use of a methane MPC subplanner during test episodes is labelled. Browser verification caught integer-widget rounding before backend validation; the recovery controls now preserve the entered value and reject fractional deadlines explicitly. Invalid inputs retain the previous simulation. The original plant and solar artwork remains byte-identical and its screenshot checks pass.
- Source `8e617d4d59b812fe9cca0a7488236b4866448744ef1a77255a8f70cbc2656f74` passes 535 Python tests, 58 Node checks and four browser checks covering the new controls, original plant/solar, keyboard inspection and all twelve Model topics. Model bindings, generated documentation, lint and formatting pass. Existing mutation checks detect every supplied corruption; the bounded reactor verifier passes its four finite models. Initial sandbox socket failures and the UI rounding failure are retained separately, not counted as passing attempts.
- Complete fixture `ee43b319d85e8d83` preserves a 20-hour zero-noise, constant-sun reset/recovery run with explicit guaranteed procedure success. Physical capacity returns at hour 6; observational confidence reaches nominal at decision boundary 10. Its source-restored numerical rerun with network access blocked matches every requested/applied action, plant state, diagnosis, observation, service record and retrospective fault state; methane and cost differences are zero. The portable bundle passes 1,605 independent checks. This is a controlled execution example, not evidence that scheduled testing beats another policy. Original-information electrolyser-off replanning is saved alongside it.
- Reporting version 3 distinguishes physical restoration, subsequent confidence, confirmation delay, test effort and solver/deadline effort. Missing restoration or confirmation leaves delay undefined. New prospective protocol `field-recovery-tests` freezes a 24-case reference comparison against opportunistic methane MPC, holding hardware and exogenous inputs fixed. It explicitly compares a package including stricter confirmation and forward service-demand accounting; no isolated optimiser effect is claimed. Sensitivities include smaller plant, smaller battery and higher observation noise. The new study has not yet supplied comparative evidence at this checkpoint.
- Information sensitivity completed 108 cases/72 pairs under frozen source c24c5830; support remains running, followed by provision and interface. Cleaning and information interpretations, sensitivity exports and required reruns remain to be completed. Full joint mission choice, conditional remedies, charging/visits/rescheduling, broader alternatives, all remaining hardware mechanisms/artwork/essays, degradation and learned-policy evaluations remain required. Stages 3–6 are still open; the six-stage goal remains active. No finding requires user guidance or a material direction change.


### Sensitivity interpretation following the recovery checkpoint

- Cleaning sensitivity now has reviewed publication `829318c466004804ac33d3e84e5725fa` (144 cases, 108 pairs). It retains the adverse periodic-cleaning seed: less modelled soiling loss coexists with 51.76 kg less methane, 1,115.63 kWh more curtailment and 57.52 kg more ending hydrogen. Feedstock-limited arms gain no methane. Treatment effectiveness, available electricity and useful plant output remain different quantities.
- Information sensitivity now has reviewed publication `19d0a71cb43049e2bc2719ebd309bce2` (108 cases, 72 pairs). Reader drift and a shared stuck contact have different evidence limits. Inspection of the shared-contact seed 19 records the same human mission completion time but different repair outcomes; the earlier reset does not repair damage. Recalculating the preserved reason-based sampler explains the differing outcomes (0.956814 versus 0.078549 with a 0.95 success threshold). This is a new derivation, not reconstructed original provenance or evidence that the faulty contact diagnosed damage correctly. A later matched-action edition is needed before isolating information value from this source of variation.
- Every original audit-artifact hash in those reviews matches. The publications retain failure, uncertainty and ending-state boundaries. Three reviewed sensitivity exports are now being prepared and independently checked from the preserved c24 source; completion must be taken from `build/services/planning/sensitivity-review/reviewed-export-results.json`, not inferred from queue presence.
- Support sensitivity completed 126 cases/63 pairs, descriptive report `d2bf41c8239648db968ddaa27f8b41c1`; its interpretation and preservation checks remain open. Provision is running, followed by interface. The new recovery reference is frozen as edition `ab21e2a591f0495fa72ebb7f665a1335` and waits for that original queue to finish before taking the store's execution lock. It has no comparative results yet.
- The app is serving tested source 8e617d4d on port 7860 with the same saved run. The recovery example also passes browser playback with networking disabled. Full scoped artifacts are in `build/services/planning/recovery-checkpoint/verification.json`. No original simulation illustration changed. The research findings narrow attribution claims and inform the next experiments; they do not require a scope reduction or user decision. The six-stage goal remains active.

### Joint dock charging and plant dispatch checkpoint

- Added `service-charge-process-mpc/1` and `service-charge-controller-port/1`. They optimize the existing plant dispatch and one shared dock against absolute robot-energy deadlines, accepted mission consumption, conditional dock/robot availability, capacity and return reserves. Beginning inventory funds this interval's robot use; charging arrives only at the end. Active service remains solar-only. This is a callable experimental port, not yet a run-level service-selection policy.
- `PlantServices.propose_charge` and explicit charging selections rebuild the installed recipe and recheck actual headroom, eligibility, power and occupancy. Booking does not credit energy. A changed decision invalidates the old comparison; rejected power requests remain recorded at their original size. Charging before a later accepted departure is allowed, without borrowing its reserved return energy. Existing local charging is unchanged.
- The exact active-hour price table retains the accumulated usage/parts allowance and separates process economics from additional dock cost. The comparison excludes unchanged service commitments and sunk prefix costs; it is not an allocated total. Missing applicable prices remain unknown. Recorded component identities, source, original parameters, estimated state, forecasts, prices and alternative restriction accompany numerical outputs.
- Source `00609a8a109d3c6e62741a21dabd1e2e625421e7ce93d82801dd6155492532d2` passes 567 Python tests (including 32 dedicated charging checks) and 58 Node checks. Initial expected documentation-freshness failures are preserved; bindings and default examples were reviewed/regenerated before the final passing suite. The two-hour executed example produces 10 kg methane with charging due at boundary 2 and approximately zero with the same target due at boundary 1. Both use 10 kWh at the dock, credit 8 kWh, lose 2 kWh and incur the declared €3 activity cost. This proves a bounded electrical/thermal opportunity, not a general controller advantage.
- The example has 373 standard-library source-integrity and independent material/electrical/thermal/robot-timing checks. Its saved-source numerical rerun with networking blocked matches physical actions, states, mission recipes, receipts and resource events exactly, with zero methane and service-cost differences. The first reproduction checker incorrectly compared in-memory tuples against saved JSON arrays; that attempt is retained, and the corrected checker compares serialized representations on both sides. No numerical differences were discarded. Artifacts and scope are in `build/services/planning/charging-checkpoint/verification.json`; the readable example is `build/services/planning/charging-example/report.md`.
- A private charger power-stage fault leaves the earlier charging decision unchanged, interrupts execution without adding energy, and prevents subsequent reliance on the dock after its failure is observed. A separate alternative preserves the original inputs while forbidding plant-battery discharge. No future fault type or successful repair outcome enters charging selection.
- The original c24 provision sensitivity completed 108 cases/81 pairs, descriptive publication `e21fa58f699d4e46a72df22ad4a97c22`. Its interpretation remains open, as does support interpretation. Interface sensitivity is still running; the new recovery reference remains queued behind it. The three reviewed sensitivity exports have not yet reported completion and must not be treated as checked bundles merely because a ZIP exists.
- Next: use these ports in meaningful work selection, observation-contingent remedies, fixed work deadlines, charging/visit coordination, rescheduling, fallback and paired Studies. Complete sensitivity interpretation, exports and numerical reruns; then continue the remaining hardware mechanisms, deep service essays/artwork and degradation/learned-policy evaluation. The original main illustration and app entry point are unchanged, and the live app remains on the earlier verified recovery checkpoint. No broader UI/performance or formal-controller claim is made for this source. Stages 3–6 and the six-stage goal remain open; no guidance question is currently required.

### Reflection: separate hardware fault physics from recovery policy before ranking support

- Reviewing the support sensitivity uncovered a causal-comparison defect. `IntervalEffects.before_stage` applies service-hardware interlocks only when `equipment_recovery_enabled` is true. The basic support arm sets that flag false. The same charger power-stage fault can therefore leave the basic arm's dock operational while interrupting the supported arm's dock. This is not a fair test of recovery value.
- The controlled reproduction on source 00609a8a saves identical 10 kW requests and active charger-fault truth: recovery disabled delivers 10 kWh and credits 8 kWh; recovery enabled interrupts and credits zero. See `build/services/planning/charging-checkpoint/support-physics-review.json`. The original c24 sensitivity seed-7 cases are `a12b0700d7b60f576c0bbeb2` (basic) and `2f23fd399bb3d36c4b46f6bf` (supported); their saved configuration, charge intervals and interruptions are retained in `original-support-physics-review.json` alongside it. These are new diagnostic analyses, not rewritten original evidence.
- Immediate priority changes: introduce an independently versioned hardware-fault execution model shared by comparison arms, separate observation/interlock state from availability of recovery actions, and test both enabled and disabled recovery policies against identical fault physics. Preserve old archives and frozen source semantics. Revise affected prospective protocols and rerun the comparison before interpreting it as evidence for or against recovery. Check other hardware families for the same coupling. The current numerical charging checkpoint remains scoped to its declared fixtures; passing its tests did not establish fairness of the older support study.
- The user has been informed. This is a correction within the agreed programme and does not require a scope reduction or further authorization. The six-stage goal stays active; the full mission supervisor, remaining hardware classes and learned-policy stages remain required.
- Published the support sensitivity scope correction as report `4034ad71076646c7bc13bfbaa5a95a75`, preserving original descriptive report `d2bf41c8239648db968ddaa27f8b41c1` and all runs. Its interpretation explicitly withholds charger-fault ranking pending an edition with matched physical mechanics. Other support conditions still need their complete interpretation.
- The old sequential driver has now finished all six sensitivity editions (702 cases). Interface completed 72 cases/36 pairs, final descriptive publication `6ff8982ea7d446afb6ef912e8c8c9565`. The waiting new recovery reference then completed 24 cases/12 pairs on preserved source 8e617d4d, report `b1b4117daa994d98a47fa79cbbbfecf8`; its actual comparative findings have not yet been reviewed. Both execution handles are finished. Sensitivity exports/checks remain active and numerical sensitivity reproductions remain outstanding.

### Independent hardware-fault physics checkpoint

- Introduced `faults.hardware_model = actuator-interlock/1`, independently of `equipment_recovery_enabled`. New plant-service records use `/11`; installed hardware interlocks and eligible command/test feedback operate even when recovery or logistics is disabled. The recovery flag continues to control compatible procedures and spares. The monitor has no fault truth or onset schedule; an unobserved asset permits an initial attempt but is not labelled healthy. Observed dock failure also constrains the new charging planner.
- Basic retrieval changes location only. `supervised-drive-test/2` checks both the configured test outcome and actual drive operability, emitting consistent readiness and hardware-monitor readings; its plan records the new implementation identity. An unrepaired drive cannot pass through a lucky test draw. Long work stages recheck hardware at hourly boundaries and retain earlier partial treatment. Cleaner, rover, charger and portable-pump cases are covered; pump failure does not disable its operator's vehicle.
- Old configurations without the new field explicitly load `recovery-gated/1`, preserving prior execution and archive meanings. Both original support-sensitivity seed-7 archives remain byte-identical and pass the applicable legacy audit. Their absent, older outcome-randomness option normalizes to the existing `legacy-reason/1` compatibility rule; this is not new matched-outcome evidence. The advanced UI defaults new supported setup to independent physics and offers a labelled legacy reproduction choice. Explicit archive setup loading retains its recorded model.
- The independent reference checks actuator-dependent work against reconstructed physical faults even when a failure receipt is omitted, validates physical drive-test outputs, monitor observation eligibility, recovery permission and model identities. New regression cases also reject forged post-retrieval readiness and prove that changing private charger truth cannot change the earlier charge selection.
- Source `cfdbca13648e8e2cdc2b553c1f1b7378f9e70b4a876eecdb2c31ef0ea5377fa1` passes 583 Python tests, 58 Node checks, four browser checks (original plant/solar screenshots, keyboard inspection, all twelve Model topics, opt-in recovery setup), plus the new physics selector on desktop and narrow screens. All original asset bytes and `app.py` match the previous checkpoint. Narrative bindings, default teaching examples and generated catalogues were reviewed/refreshed. The exact scope, first attempts and checked artifacts are under `build/services/planning/hardware-physics/`; no broad performance, formal-control or empirical validation claim is added.
- Two new 36-hour charger examples request the same first 1 kWh charge at hour 9 and both receive zero. The enabled workflow later replaces/tests one charger module and resumes charging; disabled recovery delivers none. Their standalone bundles pass 8,533 independent checks in total. Network-blocked numerical reruns from captured source match every requested/applied action, state, diagnosis, service record and private effect exactly, with zero methane and cost differences. Original examples retain the evidence snapshots applicable when captured; later validation is a separate artifact.
- Added correction reports `77dd520b6e8749e5b46204e02698704e` to reference edition `6cf7538d5009421ba80aa146962271f3` and `7f514cdfdc7e47e9aed3f56dee69eb98` to its numerical reproduction `60e5abb7c04346cbab637722912b4c10`. The earlier sensitivity correction remains `4034ad71076646c7bc13bfbaa5a95a75`. These retain all earlier reports and runs and explicitly withhold charger-fault recovery ranking.
- New support protocol revision 3 fixes physical-model matching and uses target/action/request outcome draws. Its 42-case reference edition `d0cdf955d5dd45e5b3c3d25db1626fb2` is running on the frozen verified source; no comparative conclusion has been reviewed yet. Battery-reference protocol revision 9 records the new default physical-model identity while preserving revision 8; that disabled-field fixture has no new hardware effect. Older protocols are not rewritten.
- The original recovery sensitivity export has now completed: 144 archives pass the portable integrity and full-archive checks. The same sequential driver is exporting cleaning next, followed by information. Other sensitivity interpretation, preservation and numerical-rerun work remains open. The live app is on the verified source at port 7860 with the same saved run `271835a300e75a0a`; original results were not recomputed.
- Reflection: the separation repairs an experimental-design flaw without narrowing the agreed programme. Next, review the corrected support comparison, complete the outstanding study interpretations/reproductions, and continue the actual coordinated service supervisor, remaining hardware mechanisms/illustrations/essays and degradation/learning stages. No new guidance question is required. All six stages remain the active goal; this checkpoint is not their completion.

### Comparative review and conditional cleaning value · 12 September 2026

- Reviewed and published four append-only interpretations covering 246 completed cases/150 pairs: installation/provision sensitivity (`8aa992cb3d2b41ea905e4ecf54241171`), prepared-interface sensitivity (`113760c723234797bf2a2e0f9311619f`), scheduled recovery tests (`25851d4c8453497594b22aade7b9bd87`) and the corrected support reference (`0bc1f520a4174ae78bd03036d3410eda`). Every reported case retains its passed independent audit, original source, inputs and numerical record. Original publications are unchanged. IDs, traced cases and review scripts are under `build/services/planning/remaining-review/`.
- The installation comparison exposes an essential-work problem: small-plant on-call replacement is cancelled when a bundled cleaning visit loses solar power, then is not requested again. Both compared controllers detected the initial discrepancy; this is not a detection failure. Shared/contracted resident arms have identical physical outcomes under changed price assumptions, without a multi-site fleet model. The prepared interface speeds a compatible reset but can delay replacement across the crew calendar. A failed seed-19 replacement and legacy reason-based draws limit causal attribution of the large mean trip advantage.
- Scheduled probes confirm recovery in two of three seeds in each trip/optimistic-forecast condition; the third remains unconfirmed and every scheduled trip run misses its deadline. Ordinary trip production falls while optimistic-forecast production rises. Damage remains unrepaired because the configured fixture has no human replacement asset. Differences also occur in normal runs with no probes under finite solver budgets; they are not assigned to a recovery action. No controller is required to win.
- Corrected dock-fault physics now applies to both support arms. Compatible replacement/testing restores charging, but the three-day dock comparison averages 0.068 kg less methane and €683.94 more allocated cost for support. Lower visit/expenditure counts are a different view from allocated period cost. The next decisions must value useful operation and outstanding work, not merely completed procedures or fewer visits.
- Added `conditional-section-treatment/1`, which predicts section coverage, retained pass efficacy, brush consumption, accumulation, temperature conversion and clipping from current public surface observations, registered plans and eligible raw forecasts. It never receives fault truth or private procedure outcomes. Earlier treatment evidence is required for an ongoing pass. Wet cleaning retains distinct adhered-removal mechanics; no future brush replacement or automatic rain washing is credited.
- Optional coupling version 3 and schedule-supervisor version 2 now use that conditional generation when evaluating candidates. Calls without the new references retain the earlier behaviour. Each weather outcome needs its own original raw reference, which must reproduce the corresponding untreated DC forecast. Candidate identity binds the treatment inputs; current observations stay unchanged. This remains callable scheduling machinery, not the integrated run-level supervisor. Repair/information value, interruption branches, work obligations/retries, charging/visit coordination and original-information UI alternatives remain required.
- Source `748014fff0415620dbd9f974382cd6fb3d10a6b68442e6f298440de66f534048` passes 594 Python tests, 58 Node checks, formatting/lint and current documentation bindings/evidence for the twelve existing Model topics. Eleven new checks include independent area arithmetic, dry/wet execution reconciliation, ongoing-pass replanning, clipping, missing radiation, publication timing, private-outcome separation and a bounded economic selection. Original asset files and `app.py` are byte-identical to the prior checkpoint. No new hardware illustration, empirical, formal or broad performance claim is added.
- The saved eight-hour examples select cleaning when extra generation supports production, and defer it when a 200 kW converter clips the gain. They use a deliberately illustrative €100/kg methane value, warm reactor and 300 kW array; they are not representative operating economics. Executed selected plans produce 60.5053 kg and 54.3158 kg respectively. Saved-source, network-blocked reruns match actions, states, service execution, selection context and cost views with zero methane difference. Solver elapsed-time telemetry is excluded from equality. Artifacts and the readable report are under `build/services/planning/treatment-checkpoint/`, with immutable checks under its source hash.
- Browser Use rejected opening the local example HTML under its URL policy. No alternate URL/browser workaround was attempted. Generated markup, saved data and local links were checked; visual browser review remains explicitly outstanding. This does not prevent further numerical/controller work and is not a reason to mark the overall goal blocked.
- The 42-case corrected support reference is complete. A frozen-source follow-through driver is now running the 126-case corrected support sensitivity, edition `af3946f506274c748b921ca6b601ad90`, then will numerically reproduce the corrected reference and scheduled-recovery reference. Progress is separate from comparative interpretation. The original frozen-c24 export driver still has only its 144-archive recovery export completed; cleaning then information remain in progress. Other sensitivity exports and reproductions remain open.
- Reflection: the findings strengthen the need for persistent work obligations, bounded retries/escalation, and joint repair/production scheduling. They do not change the all-six-stage scope or require a user decision. Next, integrate the now-useful candidate valuation and charging ports into an actual run-level service policy; preserve the local baseline, observed information, fixed deadlines and explicit fallback. Continue the remaining hardware families, deep service essays, comparative evidence and degradation/learned policies in full. The live app remains on the earlier verified source with its saved run; it has not been replaced by this experimental port.

### Run-level coordinated service checkpoint · 12 September 2026

- Added opt-in `coordinated-services/1`: bounded current-information mission/start proposals, joint dock/process planning, exact action-dependent service prices, original work and robot-energy deadlines, observed interrupted-repair retries and explicit escalation/fallback. The local rule remains available. Advanced setup reveals its controls; Site services reveals the recorded decision, original deadlines and candidate predictions. No injected cause, private repair result or later weather is supplied to selection.
- Two performance defects found in actual playback were corrected. Hashing an entire 40.9 MB decision prefix took a median 359 ms on the captured example. A checked projection of the exact accounting operands/source references reduced this to a 34 KB price input (projection 1.915 ms; hash 0.311 ms). The full archived trace remains intact. Sending nested candidate calculations three times made the browser payload 102 MB; the recorded display projection now sends about 2 MB and retains links to the complete stored calculations. These are fixture measurements, not a general latency guarantee.
- Future accepted starts exposed an incorrect assumption in the independent reference checker. It now requires explicit accepted selection evidence and matching start times. Original flagged editions remain flagged and preserved; a separate re-audit explains the correction. The new source-95143c93 integration edition completed 12 cases/8 pairs with independent checks from the start. Reviewed publication `4606b55f6e344392952b79d5818a104e` reports normal-operation, module-damage, optimistic-forecast and failed-dock outcomes, including the adverse failed-dock methane delta and unfinished work. Lower short-window service costs do not establish superior autonomy.
- Current source `a002e28a0efcd47c61412134092910f76b0617e0bb68ef94abc692ccd72d94ca` passes 616 Python tests, 59 Node checks, unchanged plant/solar snapshots, keyboard/narrow-screen checks, lint and documentation freshness. All twelve existing Model examples have applicable current test evidence. A separate actual-decision UI check covers disclosure, original deadlines, narrow access and return at the original hour. Scoped immutable artifacts are under `build/services/planning/coordination-checkpoint/checked/a002e28a0efcd47c61412134092910f76b0617e0bb68ef94abc692ccd72d94ca/`.
- The compact offline example retains numerical source 95143c93 and passes 2,476 independent checks. Its network-blocked saved-source rerun matches all 18 applied-action intervals, methane and cost totals. Broader equality of every diagnostic/requested/service field is not claimed by that comparison. The new 36-case reference edition `edd18fdb2b2045458098bd5a2233a641` is running on captured a002e28a; it is not yet reviewed comparative evidence.
- The frozen follow-through queue has completed corrected support sensitivity (126 cases), corrected support numerical reproduction (42 cases) and scheduled-recovery numerical reproduction (24 cases). Their comparative/reproduction differences still need review. Recovery and cleaning sensitivity exports have passed their archive checks; information export remains outstanding. The live app has been restarted with the verified controls and the same saved run, without recomputing it.
- Reflection: fixed obligations close the silent loss of interrupted work, but a crew stranded away from home remains unavailable; a queued retry cannot invent its return. Explicit crew return, joint visits, observation-contingent remedies, risk scenarios and original-information service alternatives remain necessary. All remaining hardware families, mechanism-specific essays/evidence, commissioning, degradation and held-out learning are still required. No finding changes the agreed direction or requires user guidance. The all-six-stage goal remains active.

## Interrupted crew return and computation review · 12 September 2026

- Added opt-in `interrupted-crew-return/1`: an interrupted crew packs, follows the remaining recorded itinerary, and acknowledges arrival before becoming available. It uses the same actor, route permission, finite crew calendar/hours and accompanying shared-visit cargo. Failed original work remains failed. Further interruptions during packing, travel or arrival can be retried from the last reported position. Returning creates no second callout and consumes no repair stock. Waiting outside active work and separate vehicle fuel remain explicitly unpriced; emergency rescue and damaged vehicles are outside this model.
- Verified source `936951cc60a6a560b6cce8237d6b073986d48a42015b7c3461cba4f7fd37fb92`: 630 Python tests, 62 Node checks, unchanged original plant/solar screenshots, keyboard and narrow-screen checks, recorded return animation and setup interaction, lint and documentation freshness. All twelve existing Model topics retain current numerical evidence. Fourteen dedicated Python checks cover return resources, repeated interruption, shared visits, unavailable support and independently reconstructed itineraries. The new motion reuses the original vehicle artwork.
- Saved numerical example `65f46ec4e864ea06` uses physical source `390fec6d…`; the subsequent release changes only the display of a blocked return. It passes 2,135 independent checks and a network-blocked saved-source rerun with identical requested/applied actions, states, diagnoses, observations and field-operation records. An earlier pair shows 160 kg methane in both arms; return permits replenishment and later work at additional crew cost, without a production advantage in that fixture. The example is a bounded mechanism demonstration, not a hardware recommendation.
- The 36-case coordinated reference is complete and reviewed in publication `21acf9f31cfd43bcaddab0cc5def7746`. All independent audits and hashes pass. Production differences are mixed; the economic arm leaves work unfinished and can avoid expenditure by never using the cleaner. These short-window differences are qualified, not a stable ranking. All three older sensitivity exports (recovery, cleaning and information) now pass archive integrity checks. Corrected support sensitivity has finished 126 cases, with interpretation still outstanding.
- **Material finding:** saved-source numerical reproduction changes applied actions in 23 of 24 MPC recovery cases (methane changes −40.4761 to +36.1703 kg); the forecast-stress comparison changes sign. All 42 rule-based support cases reproduce applied actions exactly. Independent physical audits pass in both cases. Append-only publications `1998ca4febba436ab94ee693cee7a123` and `61bc196c5038461a979f48cf6692750f` qualify the recovery conclusions; `58179591d36e4a008c38a3d915df6334` records the support reproduction.
- Reflection: investigate identical-decision inputs, solver work budgets and repeated trajectories before expanding hardware or treating a single MPC execution as comparative evidence. The prospective investigation is in [computation-stability-review.md](computation-stability-review.md). This changes the next priority, not the agreed all-six-stage scope. Joint visits, contingent remedies, risk scenarios, service alternatives, remaining hardware and deep essays, commissioning, degradation and held-out learning remain required. The goal stays active.


## Frozen-decision computation checkpoint · 12 September 2026

The investigation now retains a signed complete MILP, isolated repeat workers, explicit node/time settings and native terminations, independent matrix residuals, all vectors/bounds/errors, actual bundled HiGHS identity, rotating budget order and source capsules. Production solver defaults are unchanged. Source `281384a1fac7a8e452824c47ab8e3a64e20d7cb8f30402fe3bf36023172f4a32` passes 642 Python tests and current documentation checks; UI assets are unchanged from the checked crew-return release.

All 80 numerical trials (runner source `8c0d90b5…`) return mathematically feasible incumbents and repeat exactly within their problem/budget group. Four first-divergence problems reconstruct identical matrices and objectives from captured original source `8e617d4d…`. Longer budgets close two large objective gaps without changing incumbents, but all converged problems process one node; none binds its node limit. This does not establish a general deterministic computational limit, full-run stability or empirical validity. The first failed full-suite test and correction to use the declared fresh-worker boundary are preserved.

The user-facing write-up is `build/engineering/computation-stability/report.md`. Original and longer-budget trajectory editions `6b72aded9bcf463b8c788a2544c6d141` and `99ce5f6d958f472bbd999cb9ffb52854` each contain 24 cases: three identical-input executions of two recovery policies across four conditions, 72 hours with 12-hour horizons and one fixed physical seed. Their complete configuration and policy equality across repeats was checked before creation. They run sequentially through the Studies worker lease with captured source and one thread. Results are pending; interpretation must report within-policy variability before policy differences. Integrated service-supervisor outer budgets remain a separate required investigation.

The all-six-stage goal remains active, including all outstanding hardware, service alternatives, deep essays, commissioning, degradation and learned/homeostatic evaluations. Numerical repeatability is the immediate prerequisite for comparative claims, not a replacement for that scope.

## Archive collision found by repeated trajectories · 12 September 2026

The first repeated-trajectory editions are **withdrawn**: `6b72aded9bcf463b8c788a2544c6d141` has 10/24 mismatched recording references; `99ce5f6d958f472bbd999cb9ffb52854` has 16/24. Identical physical run identifiers were used as filenames even when full recordings had different case/attempt contexts or timing. Existing files were replaced. Summary metrics and audit artifacts remain, but missing complete original records are not reconstructed. Withdrawal publications are `8025b59cc9044abf823edc3834895666` and `5c6fde3a928a4caa97a86c9d62eb8541`. The 80 isolated solver trials are unaffected.

A scan of 1,574 case references across 33 indexed Studies editions found 14 incompatible paths, confined to those two editions. This does not certify every file or reconstruct unindexed standalone histories. The corrected writer includes recording integrity in new archive paths, separates study cases, atomically preserves prior files, and returns a distinct path for changed exports. Publication verifies recordings, case/attempt context and referenced audit/service artifacts before saving a complete report. Downloads select the actual returned artifact. The Model essay explains recording versus physical-run identity, and legacy unsealed files keep their original shape.

Source `bad34f9453ebde4c1526c1f11e1a119955fc9645c877f8b0198e3a426b2374e5` passes 648 Python tests and all 12 current Model topics. Assets match the earlier reviewed crew-return release exactly. Native browser reinspection was unavailable because the Mac is locked; no new screenshot check is claimed. The local app was restarted with this source and the same archived simulation. Verification and the full source capsule are under `build/engineering/archive-preservation/checked/<source>/`.

Replacement editions `29f0254e55ee412eb018879b95b1a32a` and `963a96023b9249dab282657de7c384db` use a new explicit protocol revision and captured checked source. Their complete physical configurations, policies and weather hashes were verified equal to the original planned cases. They run sequentially through the Studies lease and must each pass 24 recording checks before interpretation. Do not reuse or rebrand the withdrawn editions. The goal remains active across all six stages.

## Verified computation repeats and diagnostic reflection · 12 September 2026

Both replacement editions are complete with all 48 recording identities verified. Reviewed interpretations `5fdbbccba2b2497ea09cd3e1407ad388` and `9065c54db7a448eea30fb0cc1e739aeb` qualify within-policy variability at 0.15 seconds and matching repeated action traces at two seconds in this single-seed fixture. They correct an overbroad sentence in the preceding interpretation: the old bug did overwrite recordings; the replacement process did not overwrite surviving originals or reconstruct missing evidence. Earlier immutable publications remain available.

The two-second damage case exposes a measurement-to-control interaction: ambiguous flow balance at a successfully tracked partial load lowers capacity below minimum load, while the scheduled policy waits for a future probe. The next bounded ablation changes only that ambiguous-capacity response. Noise, thresholds, confirmation and retry timing remain unchanged; clear fault diagnosis and recovery must still work. [Mechanics and prospective comparison](ambiguous-capacity.md) records assumptions and limits. The goal remains active across all six stages; this is a prerequisite for credible comparisons, not a replacement of remaining hardware, essays, service alternatives, commissioning or learned/homeostatic evaluation.

## Checked ambiguity ablation and support interpretation · 12 September 2026

Source `9f0e5d1cef46ea9477579a5b0f885ebb9fe06b0ebe1a6ec14e0b9666d3b434d8` adds opt-in `sensors.ambiguity_policy = retain-capacity/1`; absent/default policy preserves the original complete configuration and diagnosis. The only inference change is to retain the preceding capacity estimate when informative electrical/inventory channels disagree. Advanced setup and diagnosis learning fixture version 2 expose the choice. The independent decimal checker verifies the necessary hold/status/no-new-incident invariant. Noise, counters, confirmation and recovery timing remain unchanged and their limits remain explicit.

Verification: 673 Python tests, lint/format/freshness, all twelve current Model evidence bindings, and five browser tests covering every essay control/reset, keyboard return, narrow reading, recovery setup, and unchanged original plant/solar screenshots. The first test collection naming error and correction are retained. The checked source capsule and artifacts are in `build/engineering/ambiguity-policy/checked/<source>/`. The live app now runs this source on port 7860 with saved run `271835a300e75a0a`; a read-only localhost check confirms the new setting. Current app process handle is recorded operationally, not as scientific evidence.

All 1,728 original diagnostic transitions reproduce from saved observations. The one-step alternative changes one ambiguous interval in each of three identical-input repeats. The completed whole-run ablation verifies all 48 recordings across two observer editions: `3ce3986125ba4c01b0d282ea87875a71` (original) and `4294142325fb42fc9848968e8591cba5` (hold). Every policy/condition group repeats actions within tolerance. Holding the estimate adds 71.5884 kg methane and avoids 19 below-minimum estimated-capacity intervals in the scheduled-damage case. The other seven groups retain identical actions. Allocated cost rises €78.09, assumed contribution falls €17.01, and informative tracking shortfalls rise from three to five. The permanent fault and unfinished work remain. This is not automatic recovery, an economic improvement or a general determinism claim. Original defaults remain available and unchanged. Reviewed publications: `b9e434a3c6724ac49249b5af1beae78a` and `43e9ed2253db470fa180cb94d8dcb721`; full artifacts and write-up are in `build/engineering/ambiguity-policy/`.

The outstanding 126-case corrected support sensitivity is now reviewed: publication `e8606ab062224cc892541e10f5a48cf7`, edition `af3946f506274c748b921ca6b601ad90`. All 126 original recordings and audit artifacts verify. Under the shared long-response assumption, two four-job visits plus a stock delivery replace eight standalone routine visits. In the seed-7 available-support pair this saves five callouts, nine crew hours and 9.5 travel hours, reconciling €2,410 less allocation; four purchased but unused maintenance kits make the expenditure reduction €2,210. Eight routine procedures complete in both arms, while four routine jobs and the process verification remain unfinished. Ordinary-response support performs more work at greater cost without more methane. Crew/route unavailability and exhausted stocks retain their unsuccessful outcomes. Its numerical executions and prior publications are unchanged; detailed analysis is retained inside the edition and under `build/services/planning/support-sensitivity-review/`.

Next: use these findings for new joint-visit proposals and recorded service alternatives, then the remaining observation-contingent/risk planning and hardware families. Evidence-run budgets must be explicit: two seconds per process solve showed repeated trajectories in this fixture; the integrated service supervisor's separate outer candidate budget still needs its own computation qualification. Additional observer seeds/boundaries remain a scope limit, not a claim already checked. The new support interpretation's portable export and other outstanding source-bound reproductions remain stage-3 gates.

Storage observation: this workspace currently holds about 17 GB under `runs` and 52 GB under `build` (47 GB of the latter under service evidence), with about 33 GiB free on the host. No old evidence was deleted. Avoid unnecessary duplicate extraction when verifying future large bundles; consider streamed checking or verified disposable-copy cleanup before producing further large batches. This is an operational resource constraint, not a reason to erase historical evidence or reduce the agreed six-stage scope. All six stages remain the active goal, including deep service essays, every hardware family, commissioning, degradation and held-out learned/homeostatic evaluation.

## Planned shared crew visits checkpoint · 12 September 2026

Source `7a78c6b2e36edd46a27493b10b65db678ad1c9551b6afd805130987e7cbd6aa5` adds opt-in `coordinated-services/2`, current-information shared-visit recipes, combined demand/charging/production interfaces and explicit accepted-group records. Single and shared proposals are interleaved; the finite pool, omitted candidates, feasibility failures, solver terminations and original work deadlines remain visible. Each shared itinerary reserves the complete crew/tool envelope and current stock, retains separate effects and verification, and requires a full shift and return. No successful restoration or future stock is credited. [Mechanics, interfaces and limits](service-coordination.md).

The saved prescribed-trip comparison executes two half-hour dock procedures with separate quarter-hour outward/return journeys. Two separately requested trips cost €880; one shared trip costs €530, reconciling one avoided €300 callout and half an hour at €80 crew plus €20 vehicle travel. Both kits and separate procedure completions remain. The short-shift example is explicitly infeasible. All 645 recorded checks pass. This is a mechanism fixture, not a closed-loop policy ranking or a claim that optional routine work extends life.

The first export exposed a real boundary mismatch: the projection admitted two distinct future crew departures, while execution accepted only the first. That failed artifact is retained. Current projections explicitly require one crew journey per candidate; another separate departure needs an observed return at a later decision. The corrected comparison follows that sequence. A second correction versions candidate pricing as `/2`: registered routine work consumes a kit but has no delivered destination inventory; actual or unknown supply effects still retain their acceptance condition. This can change candidate eligibility for version-1 coordination too, so old source-bound runs retain their original semantics and any numerical differences must be reported.

The initial 30-hour closed-loop challenge is retained under `build/services/planned-visits/exploration/`: both scheduler arms produced 228.090909 kg methane and made one separate visit because the two requests became known at different times. Replacement remained unverified and calibration queued. Its original source is `7a6ce0fca90009267b9b58e420ee8e71648d72ce8021fc149683b8ce0f6faf9a`; it is a single constant-weather/zero-noise negative example, not a general equivalence claim. It cannot justify retrospectively adding a later request to an earlier visit.

Verification at the checked source: 693 Python tests, 62 Node checks, all twelve current Model numerical evidence bindings, lint/format/catalogue/freshness checks, and three browser checks for the opt-in keyboard/narrow-screen control, quiet viewport and unchanged original plant/solar screenshots. The first browser attempt pressed ArrowDown twice and wrapped to the original choice; the corrected keyboard test passes. The report's first narrow-screen check caught a long-source-identifier overflow, which is corrected and visually reviewed. Failed attempts remain under `build/services/planned-visits/checks/`.

Captured-source reproduction matches every service-event field, quantity, cost amount, check, original input and predicted action except two solver duration fields and their correctly recomputed cost-trace identities. Full recordings deliberately retain distinct integrity hashes. This small restored fixture does not establish general optimizer determinism. Current saved examples, source capsule and human-readable report are under `build/services/planned-visits/recorded/7a78c6b2e36edd46a27493b10b65db678ad1c9551b6afd805130987e7cbd6aa5/`; the reviewed report is `report-baf97d8e2ae95b03a3f46b8be49fdd7be22467585be48b0aa9116bb99b770056.html`. It opens without network access and preserves the earlier exploratory recording.

The all-six-stage goal remains active. Shared visits complete one missing stage-4 mechanism, not the stage: observation-contingent remedies, risk-sensitive service/production planning, targeted service alternatives and interpreted matched studies remain required. Stage 3 still has outstanding sensitivity exports/reproductions. All remaining hardware mechanisms, service Model essays, commissioning, degradation, predictive maintenance and held-out learned/homeostatic policies remain in scope. The findings support proceeding; no change of direction or user guidance is required at this checkpoint.

The local app was restarted on port 7860 with this checked source and the same saved run `271835a300e75a0a`. Read-only `/config` verification confirms both scheduling alternatives are available, with version 1 still selected by default. The recorded numerical report is available in the app.

### Compatible procedure alternatives checkpoint

Checked source `349642a833b66b956cf464d9449e09f6ce4062b028a064e997142c0e569bdf5b` extends the original-information comparison with fixed/rover contact reads and resident/portable section cleaning. `service-planning-snapshot/2` records only installed compatible alternatives at the original boundary. Version-1 snapshots remain usable for their existing alternatives and explicitly lack original procedure choices. The selected target, request/evidence, departure, other work and accepted commitments remain fixed; resource and shared-itinerary constraints are rechecked. A reading earns no hypothetical finding or recovery. The full simulated run is immutable.

The actual three-section example caught a generic action-match error that a single-section fixture did not expose: another section's duration could be offered for the target. Matching now requires that exact section's registered capability, with an unequal-area regression check. Keyboard focus now moves to Cancel during a calculation and returns when it finishes, without stealing focus from another edited input. Candidate pricing `/3` also corrects fixed dock charging's inappropriate material-delivery acceptance condition. Known dock activity/input electricity can be priced; energy receipt remains conditional. Unknown purchased-material acceptance and missing prices still leave costs undefined. A physically feasible comparison with unresolved costs is explicitly incomplete. These changes can alter current candidate eligibility, while archived decisions retain their original source and pricing identities.

Two eight-hour executed examples and their six-hour H0 predictions are preserved under `build/services/procedures/recorded/349642a833b66b956cf464d9449e09f6ce4062b028a064e997142c0e569bdf5b/`, with `report.html` as the readable entry point. The wet procedure increases available DC from 4,224.333 to 4,420.778 kWh, but both alternatives predict 40 kg methane. Its service decision cost is €465.08 versus €5.958 for resident work; it uses 26.667 L of water. A zero-water case preserves the baseline and leaves the wet alternative infeasible. These are conditional original-information mechanism examples, not annual economics or a controller ranking. They reinforce evaluating a treatment at the actual production constraint rather than equating recovered solar with useful output.

Verification: 744 Python tests, 64 Node checks, six browser checks including the unchanged original plant/solar references, all twelve source-matched Model evidence bindings and 2,539 independent archive checks pass. The actual worker/UI summary matches the saved calculation; infeasibility, keyboard return, narrow access, report links and offline playback are checked. Restored-source reruns match all eight applied intervals, methane and costs for both examples. Full serialized original-information packets and comparison responses reproduce with no excluded numerical, source, solver or constraint fields. The real prior `/1` snapshot archive still replans postponement, reports changed source and offers no reconstructed procedure choice. Earlier failed attempts and pre-evidence recordings remain retained; no archive or prior publication was rewritten.

The main app remains on port 7860 with its original loaded archive. The new checked example is separately available on port 7862. The all-six-stage goal remains active. Observation-contingent inspection/remedy planning, original recovery-test preservation, local reserve alternatives, risk/terminal studies, remaining exports and sensitivities, complete service Model coverage, the other hardware mechanisms, commissioning, degradation and held-out learned/homeostatic policies all remain required. No finding requires user guidance or a change in direction at this checkpoint.

### Conditional investigation, interrupted reads and portable learning cases · 12 September 2026

- Added `contact-investigation-belief/1`, `conditional-service-work/1` and `inspection-contingency-planning/1`. These compare qualified replacement with an inspection whose actual possible finding changes a compatible conditional remedy. The prior, static reader offset/dropout hypotheses, finite noise quadrature, context, procedure reliability, forecast, costs, component identities/parameters and requested/delivered trajectories are recorded. No private fault type, future sample or outcome draw enters the calculation. A noiseless closed contact retains the common-contact hypothesis (5/7 trip versus 2/7 stuck contact under the starting assumptions).
- Guarded work remains prediction-only and cannot enter execution. A future crew request begins its response lead when the finding becomes available. Current stock, tool peaks, return margin, isolation, accepted work and whole journeys are retained. A second separate crew departure needs an observed return. Optical forecasts preserve original radiation and accepted treatment; missing references are not replaced with constant soiling. Scenario solver version 3 adds observation-dependent request bounds without changing version 2 calls.
- A mobile inspection interruption now has its own branch, at the execution adapter's perform-completion boundary before sampling. It retains spent energy/materials and outstanding rover recovery but does not invent verification, a contact reading or return. Independent execution comparisons reconcile both completed and interrupted missions and their decision costs. A procedure's hidden success changes conditional delivery only; requested actions remain shared for identical information histories. Planned consecutive tests do not confirm recovery, and the original derated estimate remains after their predicted window.
- Static priors have an explicit epoch. An observed intervention cutoff requires a declared new prior or a separately validated transition model; old evidence is discarded without silently resetting the original prior. Unsupported findings, missing continuation resources, unpriced applicable inputs and pre-existing nonconstant capacity/test commitments remain explicit gaps. The new calculation is not yet connected to a run-level investigation policy or the live service what-if interface.
- Saved six twelve-hour learning cases under `build/services/investigation/recorded/7b748ef2e463c2cc736e8e6c256c9d1b984cee189cfc73e4cf58014af8d44764/`: fixed reader, mobile interruption, common-contact sensitivity, reader dropout, late crew and insufficient power. Four complete paired calculations and two incomplete cases retain all positive-probability branches without renormalization. The report, original packets, full predictions and source capsule work offline. Recalculation from restored source with networking disabled reproduces all six saved JSON results, excluding measured solver duration and its two dependent output hashes. The first checker incorrectly compared Python tuples with decoded JSON lists; its attempt is retained and the corrected full saved-JSON comparison is `restored/reproduction-serialized.json`.
- Source `7b748ef2e463c2cc736e8e6c256c9d1b984cee189cfc73e4cf58014af8d44764` passes 768 Python tests, 64 Node checks, 66 browser checks (13 fixture-conditioned skips), formatting/lint for 225 Python files, all twelve current Model bindings/evidence and 28,439 independent reference checks across 47 validated predicted branches. Original plant, solar and existing Model screenshots passed without changing references. The new offline report was reviewed on desktop/narrow screens; every case selector, local link and keyboard table scroll passed. Exact artifacts are in the corresponding `checked/` source directory.
- The broader browser run exposed a pre-existing focus race: an old requestAnimationFrame menu/inspector callback could move focus after the next keyboard target was selected. Focus now follows the synchronous render, with a deterministic queued-callback regression. The failed earlier `65cbbb3e…` attempt is retained. The main app at port 7860 now runs the checked source with the same original archive `271835a300e75a0a`; its missing original service context stays explicit. A live walkthrough verified keyboard entry/return and the current Model passage. The new investigation examples do not change the main run's decisions or artwork.
- Reflection: the shared-contact-heavy fixture predicts 88.364 kg methane and −€84.92 contribution for inspect-first, versus 73.636 kg and −€1,015.89 for immediate replacement. This apparent short-window advantage partly reflects avoided replacement while faults remain unresolved. It does not establish controller superiority or sustained recovery. All test outcomes remain unverified at this prediction boundary. This supports the existing requirement for executed follow-through, uncertainty transitions and terminal work/health conditions; it does not change the agreed programme or require a guidance question.
- Next: connect the bounded branch machinery to actual observation-conditioned requests, joint charging and existing test commitments, verification-driven continuation, original-run inspection/what-if transport and executed matched studies. Complete remaining comparative publications/reproductions, field Model workflows, all remaining hardware mechanisms/illustrations and degradation/training/held-out policies. All six stages remain the active goal; this is a checked calculation/learning checkpoint, not their completion.


### Measured post-service follow-up checkpoint

- Added opt-in `coordinated-services/3`, `post-service-load-evidence/1` and `bounded-post-service-followup/1`. Actual upward load requests, electrical measurements and independent inventory balance support a separate qualified substitution only after repeated, resource-feasible post-return shortfalls. Inactivity, ambiguity and known resource limits do not authorise a repeat. Retry timing starts at the first sufficient evidence boundary; later failures cannot move it. Original work deadlines and finite attempt budgets persist, and exhausted budgets remain escalations. Earlier procedures are not rewritten as successful.
- The new `followup_unverified` execution port requires a current intact assessment and a completed unverified substitution, creates a distinct work order with fresh prerequisites/response lead, and links its original obligation and test evidence. Completed-but-unverified work remains distinct from interrupted missions and observer-confirmed recovery. Versions 1 and 2 retain their behaviour.
- The revealed Site services pane includes post-service tests and their original operands/model parameters. Model explains the rule. A walkthrough caught a focus gap after selecting a Model topic; the topic change now focuses the visible Return control, with a dedicated immediate-Escape regression. The original scene and solar screenshot references are unchanged.
- Final checked source: `fc2faa99688e675ff7d12f9cb1c0fa013742265277e5937ad3b8445c4bda2612`. The complete Python suite passed 788 tests on the immediately preceding source `5e70532ede719429425898195af8569f6206e5c7c731c70447540155dac8ead1`; its only subsequent source-identity difference is `assets/model.js`. Final-source checks pass 72 relevant Python tests, 65 JavaScript tests and 67 browser checks; 13 optional-fixture browser tests are skipped. All 12 teaching evidence topics are source-matched. No previous formal, mutation or active-batch performance result is promoted to the new source.
- Four 40-hour recorded cases at `build/services/verification/recorded/fc2faa99688e675ff7d12f9cb1c0fa013742265277e5937ad3b8445c4bda2612/report.html` cover failed substitution with follow-up, a version-2 baseline, successful substitution and loss of power from H14. They pass 17,164 independent physical/economic checks and 2,118 additional standard-library checks of original observed operands, residual arithmetic, post-return timing, distinct evidence, attempt limits and retained deadlines. Each source bundle restores and checks offline. All four final numerical reruns preserve requested/applied actions under the recorded tolerance, diagnosis dictionaries and listed public work-order histories. These are workflow cases, not a completed service Study programme or empirical maintenance model.
- Reflection: repeating an ineffective substitution gives 260.636 kg methane and €2,696.31 allocated cost, versus the single-attempt baseline's 290.909 kg and €1,687.63. Successful repair also produces 290.909 kg because CO₂ limits the window, but reaches 450 kW confirmed capacity and no outstanding obligation; the failed baseline stays at 225 kW with unresolved work. Methane alone masks operational health. An earlier baseline numerical rerun differed by +9 kg under the finite solver limit; that comparison remains preserved despite the final matching reruns. Cause-aware action selection, terminal uncertainty/work and longer comparisons remain necessary. No material direction change or user guidance is required.
- A completed-case label means the experiment reached its end, not that recovery succeeded. The comprehension notes distinguish that from observer confirmation and distinguish a permitted repeat from a useful predicted repair. Desktop/narrow report and service views were reviewed; the main app retains archive `271835a300e75a0a` on port 7860. Evidence and reproduction details are in `build/services/verification/checked/fc2faa99688e675ff7d12f9cb1c0fa013742265277e5937ad3b8445c4bda2612/review.md` and `verification.json`. The sample Model interaction p95 is 190 ms and rendering p95 is 0.6 ms; this does not close the active-batch or solar latency gates.
- Next: connect the conditional investigation comparison to actual action selection and subsequent findings, retain valid posterior epochs/transitions after intervention, and preserve accepted charging/work and scheduled recovery commitments jointly. Keep failed/unsupported alternatives, terminal health uncertainty and unresolved retrieval/work visible. Remaining stage-3/4 publications, all unfinished hardware families and deep service essays, and stage-6 degradation, training and held-out learned/homeostatic policies remain part of the active six-stage goal. This checkpoint does not complete a whole stage or the programme.

### Observed investigation execution checkpoint

- Source `aa562f6618f9c3e292290e6ceb359225c8a86624133d554e16c8f95f830cc622` adds an opt-in structured investigation policy, private candidate previews, actual finding-conditioned requests, reset-inclusive measured follow-up and an episode deadline that does not restart with each intervention. Unselected candidates create no real work or reservations. Only commissioned readers contribute to the new evidence interpretation; old configured-reader semantics remain unchanged. Unsupported findings and incomplete comparisons retain explicit uncertainty. Physical recovery still requires observer confirmation.
- Seven 32-hour execution cases are saved in `build/services/investigation-integration/recorded/first-check/report.html`. In the matched damaged-equipment cases, inspect/reset/follow-up produces 223.909 kg and confirms H26; direct replacement produces 236.909 kg and confirms H19. Both record €530 service expenditure. This one declared, noiseless fixture does not rank the policies generally. The unreadable, late-evidence and power-shortage cases remain escalated rather than claiming recovery. All endings and unsuccessful outcomes are retained.
- Verification passes 128 final-source targeted Python tests, 66 JavaScript tests and 67 browser checks, with 13 optional browser fixtures skipped. An earlier full run passed 801 tests; two identity checks failed because the example writer's import changed during that run. Final-source targeted tests include and pass both. The machine-readable source difference contains only that import correction. This is not represented as a clean full-suite run at the final source. Initial legacy-checker and documentation-binding failures were corrected and their logs retained.
- All seven saved cases pass 31,938 independent checks in aggregate and restored dependency-free bundle checks. The matched damage pair also reproduces from captured source with networking disabled: all 32 applied intervals match and methane/cost deltas are zero. The other five numerical reruns are not claimed. All twelve current teaching-evidence topics are source-matched and passed. Original plant/solar and existing Model screenshot references are unchanged. The new offline report and on-demand Site services investigation view pass desktop/narrow, keyboard-return and reduced-motion checks. Model interaction p95 in the browser fixture is 84 ms; rendering p95 is 0.5 ms. No active-batch or broad hardware performance claim follows.
- Browser startup initially failed under sandbox and then during local readiness. The permitted retry after the large numerical run passed. The main app's old loaded archive was not replaced; the newly checked cases have their own offline playback. The report was opened in the app. Detailed scope and configuration are in `docs/observed-service-investigation.md`; checked artifacts are under `build/services/investigation-integration/checks/final/`.
- Reflection: actual findings now lead to actual compatible work, but the comparison's future test schedule remains a prediction. Joint future test/charging/work preservation, validated post-intervention belief transitions, a policy editor and original-information investigation alternatives remain unfinished. Remaining multi-seed publications, hardware mechanisms/illustrations, complete service essays, commissioning, degradation and held-out learned/homeostatic policies remain required. This checkpoint completes neither a whole stage nor the six-stage programme.
- Platform status: the existing goal was already marked **blocked** following an earlier model-capacity/compaction failure. Its full objective is preserved. Attempting to create the same objective was rejected because an unfinished goal already exists; it has not been falsely marked complete. Goal resumption is controlled by the app's progress row. This is a continuation-control interruption, not a finding that reduces the project scope or a request for new implementation authorization.

### Joint accepted recovery windows, work and charging · 12 September 2026

The user resumed the goal successfully; its status is active. The earlier platform compaction/capacity interruption is no longer blocking progress. The full six-stage objective remains unchanged.

- Added opt-in recovery policy `scheduled-load-tests/2`, the joint service/recovery port and scenario planner version 4. Ordinary and scenario charging share the same constraints and independent fleet reconciliation. Candidate service work, robot charging and process requests now retain accepted test windows, isolation, beginning inventory, return reserves, exact pooled dock costs and original deadlines. Before eligible observations distinguish branches, charging and requested process actions remain equal. Neither the scheduler nor the joint optimiser receives fault type, private procedure success or future receipts.
- The operating executive retains an accepted absolute test window across replans. Real consecutive evidence consumes its duration. Failed tracking, changed observed capacity, eligible service receipts and unresolved continuation can release it with a recorded reason while retaining the original episode deadline. A missed deadline is reported before a subsequent retry episode. A missing combined incumbent leaves recovery unresolved and uses the existing feasible essential-work fallback. The policy is not a global fleet or calibrated fault-probability solution.
- Process and service alternatives now preserve the original joint request, accepted test and robot deadlines. Process replanning replaces the original planned dock demand rather than counting it twice. Recorded examples are immutable. Old scheduled-test archives without joint inputs retain their explicit service-alternative limitation. A new advanced setup choice explains that coordinated MPC uses the joint policy while Greedy retains the independent scheduler/local service baseline; exact policies are saved in provenance.
- Added the collapsed **Reserved recovery test** disclosure in Site services and updated the Model controller explanation. No original plant, solar or repair artwork changed. `docs/joint-recovery-planning.md` specifies timing, economics and boundaries. Conditional test schedules inside the earlier investigation comparison are still predictions until the operating executive accepts a window; post-intervention belief transitions and an investigation-selection what-if remain unfinished.
- Checked source `a019315220094ac5ee79586e6752b76e2adb3e460e79a909a8ecc5fcddf5266a` passes all 817 Python tests, 67 Node checks and 67 browser tests (13 optional fixture-conditioned skips). Fourteen new checks cover joint charge/test feasibility, independent energy/loss accounting, accepted windows, failure/receipt deadlines, stale acceptance, original-information alternatives and future-information independence. Lint, formatting, documentation freshness and all twelve current teaching-evidence topics pass. Original plant/solar/Model screenshot references are unchanged. Model browser-fixture p95 was 190 ms input-to-result and 0.7 ms rendering; this is not an active-batch performance claim.
- Published four matched 24-hour examples under `build/services/joint-recovery/recorded/verified-a019315220094ac5ee79586e6752b76e2adb3e460e79a909a8ecc5fcddf5266a/`. `write-up.html` is the readable entry; `report.html` retains full timelines and case links. The first export, correctly showing stale teaching evidence before the evidence pointer was updated, remains separately preserved in the source-named directory. The verified archives contain matching documentation snapshots and pass 14,706 independent checks in aggregate.
- All four source-restored bundles pass the dependency-free offline checker. Networking-disabled numerical reruns reproduce all 24 applied intervals exactly for normal-independent and both shortage cases. Normal-joint differs at H22: 270 rather than 315 kW electrolyser load, within the solver's declared optimality tolerance. Both trajectories pass independent checks; methane delta is zero, allocated-cost delta is −€0.0703636, and ending hydrogen is 2.272727 rather than 3.090909 kg. The difference, solver records and ending inventories are retained; exact numerical reproduction is not claimed for that case. Both original-information service/process replan checks preserve accepted starts and source runs.
- Reflection: normal-independent produces 178.909091 kg methane versus 177.272727 kg for normal-joint. Both end at a 315 kW capacity estimate. The shortage pair each produces 126.349005 kg and ends estimated at 225 kW. Joint scheduling records two missed test deadlines in each case; the independent arm records zero and one under its different rolling-window semantics. None confirms full capacity inside 24 hours. These findings support honest deadlines and longer follow-through comparisons, not a claim of production superiority. No finding requires reducing scope or user guidance.
- Desktop/narrow report links, offline playback, keyboard disclosure/return and reduced motion were checked; screenshots and detailed verification are under `build/services/joint-recovery/checks/`. The local app is running the checked source on port 7860 with the same original archive `runs/methane-v2/271835a300e75a0a.json.gz`; read-only configuration verification confirms the new selector. Current process session is 88153 (PID 94217), recorded only as operational context, not scientific evidence.

The goal remains active. This checkpoint advances stage 4 without completing it or the six-stage programme. Remaining contingent-plan acceptance and belief transitions, broader matched/terminal-condition studies and exports, full service Model coverage, the other hardware mechanisms and illustrations, commissioning, degradation, predictive maintenance and held-out learned/homeostatic policies remain required. Do not repeatedly revalidate this frozen checkpoint unless a new change, failure or unresolved concern warrants it.


### Observed investigation continuations and accepted tests · 12 September 2026

Source `12095a62173dfcca6451dc529218840d9bc832346416db42edda0cc5e5906640` adds opt-in investigation version 2, a bounded conditional test target, immutable finding-level continuation records and joint acceptance after actual/proposed work and complete return. Every positive-probability latent branch must agree before the original continuation is nominated; private restoration outcomes never select a branch. Actual interruption or distinct eligible post-service shortfalls can justify follow-up without renewing the static prior. Original investigation and earlier recovery deadlines remain explicit. [Mechanics](investigation-continuation.md).

- Joint candidate evaluation now requires the nominated intervention, its accepted commitment, or an eligible actual completion receipt. Shared journeys include later jobs and the whole return. Interrupted work cannot authorise its old post-return test. Snapshot version 3 records public work receipts and observed visit returns; version-1/2 archives still load but do not reconstruct missing receipts from planned routes. Process/service alternatives preserve the original prerequisite and accepted test. Investigation-strategy alternatives and calibrated post-intervention belief transitions remain unfinished.
- Advanced field setup has a revealed investigation editor for strategy, reader, follow-through, risk, comparison budget, minimum assumed restoration probability and observation wait. A custom-prior roundtrip check preserves recorded hypotheses and changes only the selected risk setting. The Site services episode displays original predicted and accepted test windows separately. Original plant/solar/Model illustrations are unchanged. New public telemetry fields carry observation timing; they do not expose successful repair truth to the controller.
- Verification: all 830 Python tests and 67 Node checks pass; lint, formatting, documentation freshness and all twelve source-matched Model evidence topics pass. The full browser suite passes 65 checks initially and encounters three `ERR_ADDRESS_INVALID` local asset-load failures before mounting the app. Those failures and a subsequent server-readiness failure are retained. The final targeted rerun passes all three affected checks; 13 optional browser fixtures remain skipped. This is not described as a single clean full-browser invocation. The new investigation editor and desktop/narrow offline report/disclosure/return checks pass.
- Six 32-hour cases are saved under `build/services/continuation/recorded/12095a62173dfcca6451dc529218840d9bc832346416db42edda0cc5e5906640/`. `write-up.html` interprets the results; `report.html` links full timelines, recordings and bundles. All six recordings pass 32,311 independent physical/accounting checks and 329 additional timing/observation checks. Each source-restored bundle passes its dependency-free offline checker. Two recorded process/service alternatives retain the accepted H8 test and source immutability.
- Original recordings: trip-separate 260.909091 kg, confirmation H18; trip-joint 250.909091 kg, H19; damage-separate 223.909091 kg, H26; damage-joint 211.345453 kg, H29. Both matched pairs end estimated at 450 kW, but inventories and thermal states differ. Unreadable and shortage cases remain unconfirmed at 225 and 360 kW. These are seed-7, noiseless, illustrative workflow fixtures, not an empirical reliability study or isolated ablation.
- **Reflection changes the next order of work:** networking-disabled numerical reruns reverse both paired methane rankings. Trip-separate changes 17 applied intervals from H14, −26.428507 kg and −€18.507396 allocation. Damage-joint changes five intervals from H24, +19.563638 kg and +€13.184728. Shortage-joint changes four intervals from H13, +0.117222 kg and +€0.053395. Trip-joint, damage-separate and unreadable-joint reproduce all 32 applied intervals within tolerance. Every numerical rerun matches captured source and passes independent checks. At the first differences, original process solves are time-limited (gaps approximately 3.2%, 50.0% and 3.1%); two reruns solve within tolerance and the third reaches a smaller time-limited gap. Both original and recomputed endings and solver records are retained in `checks/reproduction-differences.json`. These recordings establish mechanics, not a stable policy ranking. Qualify process and outer-comparison budgets/repeated execution before further ranking studies, then isolate test timing under matched targets/deadlines. This follows the existing assurance scope and needs no new user authorization.
- The main app is running the checked source on port 7860 with the original saved archive `271835a300e75a0a`. Initial restart failed its local-only health check. A diagnostic retry succeeded with HTTP 200 after 9.0186 seconds; current configuration exposes the new controls. The runtime wrapper only records HEAD timing and preserves validation; it creates no public share or physical-model change. Current handle 98949, PID 3482. Cold-start margin near the ten-second allowance remains an engineering concern. All numerical/export/browser workers from this checkpoint have finished.

An agent-led comprehension review distinguished completed execution from confirmed recovery, nominated from accepted windows, recorded observations from predicted return times, and workflow verification from stable policy performance. The initial temptation to interpret the original ranking was corrected after numerical reruns; this misunderstanding is recorded separately from test results. No external participant study is claimed. Artifacts, source difference, failures, screenshots, original-information replans and verification hashes are under `build/services/continuation/checks/`. The generated current model catalogue was refreshed after the verified recordings; their original captured documentation remains preserved.

The full six-stage goal remains active and incomplete. Budget qualification, broader matched/terminal studies and portable publications, dynamic belief transitions, the investigation-selection what-if, complete service Model essays, unfinished hardware mechanisms and illustrations, commissioning, degradation, predictive maintenance and held-out learned/homeostatic training/evaluation remain required. Do not repeatedly revalidate this frozen checkpoint without a new change, failure or unresolved concern.

### Repeated computation through Studies · 12 September 2026

Checked source `95f11a4503d83d1f5b11a7f8f37c10d512d1b18fa8050d10ff56fb770744234b` adds `repeated-field-computation/1`, a declared three-budget comparison and distinct unchanged-seed repetitions through the existing Studies worker, cancellation, publications and preservation contracts. [Method and boundaries](computation-qualification.md). The original physics, controllers, plant/solar illustrations and ordinary dispatch defaults are unchanged.

- `field-computation-v1.json` defines a 36-case, 32-hour reference: resettable trip versus equipment damage with a stuck contact, separate versus joint test packages, three computation budgets and three repeats of seed 7. The constant 750 kW weather fixture is upstream of the existing lumped soiling loss, at 20°C. An active explicit solar design or optical section basis is rejected before resolution rather than silently changing the experiment. Other declared plant-capacity changes retain the complete-basis rules. Policies and configurations now compare tuple/list representations through their canonical JSON form while still rejecting implicit defaults.
- Computation packets seal original numerical actions, state, diagnostic estimates, public work history, process solver results and service selection status with input/source/weather identities. Shortened, nonfinite, missing and mismatched repeats cannot appear stable. Reporting, reuse and portable checking reject a packet that differs from its sealed archive, including a missing packet. Repetitions are not pooled as additional environmental seeds. Source-bound Model narrative and the Study UI explain the distinction between repeatability, process solve quality and outer-search limitations.
- Final verification passes **841 Python tests**, **67 Node checks**, **69 sequential browser checks** (14 optional-fixture skips), two additional actually executed computation-fixture browser checks and one preview check against the user's real local app. Lint, formatting, documentation freshness and all twelve current source-matched teaching-evidence topics pass. The initial source passed 839 Python tests; the first stricter solar-basis check then exposed a resolver-order mistake. Its targeted failure and an interrupted full run (158 passed, one failed) remain preserved. The final source corrects that issue. An overlapping browser run had two five-second Model-comparison timeouts and one interruption; the subsequent sequential run passes. These failures and source differences remain under `build/services/computation/checks/`. No old visual reference changed, and no active-batch performance claim is made.
- Reference edition **`a3d2bfb295e649b89e93d33354700f7f`** completed all 36 cases, 18 matched comparisons and **194,562 independently calculated physical/accounting checks**. Every original recording passes publication integrity checks. The generated publication is `2b28a08c1e394a9ea5201e4ee50aac93`; agent-authored interpretation `285fcd194f1b486f860063b05b4cbdfa` retains those exact attempts. The worker uses one numerical thread on a shared workstation. Its execution plus preservation took 536.38 seconds; individual simulation calls took approximately 3.99–10.56 seconds. No exclusive or idle-host claim is made.
- For each fault/policy combination all nine executions across the three budgets have exactly equal recorded numerical vectors and public work histories. All 1,152 executed process solves meet the requested 0.001 relative-gap tolerance; the largest recorded gap is approximately 0.000333. This does not prove global optimality or cross-platform determinism. Every damaged-equipment joint case still records a service fallback at interval 18: no validated combined incumbent, then feasible essential work/local charging and process dispatch against actual commitments. It is preserved rather than hidden by the passing process solves.
- The trip package comparison is 234.480584 versus 250.909091 kg methane, separate versus joint: +16.428507 kg, +€11.578759 allocated cost and +€4.849748 assumed operating contribution. Damage gives 223.909091 versus 230.909091 kg: +7 kg, +€10.210900 allocated cost and **−€3.210900 contribution**. Both packages end at a 450 kW capacity estimate with no active incident or unfinished/unverified restoration work; confirmation is available at H18/H19 after the trip and H26/H29 after damage. All end with 400 kWh battery energy, but gas and thermal inventories differ. More methane does not establish earlier confirmation or a better economic outcome. The higher budgets do not change this sample and do not justify raising defaults. The result does not isolate why earlier standalone reruns differed.
- **Reflection changes the next immediate engineering task:** the full reference publication and transport include excessive repeated service details. Its JSON is about 104 MiB and HTML about 108 MiB. The short browser fixture did not reveal this scale. Original publications remain intact. A reviewed **114,873-byte reading copy** at `build/services/computation/write-up.html` renders the exact interpreted Markdown, embeds Departure Mono, links the complete original and passes offline desktop/narrow/keyboard checks. This is a usable reading artifact, not a claim that the standard large-report UI is fixed. Next, make the standard presentation and transport load detailed case/service records on demand, then finish the full reference bundle and fresh-worker reproduction. Do not repeat physical experiments merely to change report presentation.
- Frozen Study worker Model snapshots correctly mark their original teaching-test artifacts missing. The source-bound pytest/browser/evidence artifacts are preserved separately in the edition's `verification/` directory; those original snapshots have not been relabelled. The four-case short fixture passes export, standalone checks and packet tamper rejection. Full-size reference bundle export/checking and a new-source-restored worker reproduction of this 36-case edition are **not yet claimed**. The original execution already ran from its captured source. Main app session **3950** serves the checked source on port 7860 with the same archive `271835a300e75a0a`; the live preview passes. All computation and acceptance workers from this checkpoint have finished.

The agent-led comprehension record distinguishes repeats from seeds, process termination from service fallback, production from economic/terminal outcomes, and original evidence from later verification. It is not a participant study. `build/services/computation/checks/verified/verification.json` records checks, failures, preservation boundaries and artifact hashes. The reporting-size finding changes work order within the existing assurance scope and does not require reducing the agreed programme or new authorization.

**The full six-stage goal remains active and incomplete.** After the report/transport correction and outstanding preservation checks, continue the broader matched and terminal-condition studies, investigation alternatives and bounded belief transitions, complete service Model coverage, all unfinished hardware families and illustrations, commissioning, degradation, predictive maintenance and held-out learned/homeostatic training and evaluation. This checkpoint completes neither a whole stage nor the programme.

### Large-study reading, preservation and independent reproduction

- The Studies workspace now reads a small projection of the original publication, retaining its displayed figures, comparisons and ending resources. The 36-case original JSON is 109,289,209 bytes; its display projection is approximately 1.4 MB (the actual compact transport was approximately 1.32 MB). Detailed service work loads on demand, bound to the original edition, report, case, attempt and controller. The attempt's integrity identity must also match the publication. Superseded or failed requests retain navigation and cannot replace another selection. No numerical cases were rerun to create this projection.
- Completed-edition listings use verified publication summaries and label their counts as published counts; running, interrupted and withdrawn states remain distinct. Derived files preserve the original report hash, are cached separately, and are published atomically so simultaneous readers cannot encounter half-written data. Changed or missing originals/derived files are rejected; read-only stores can derive a view in memory. Existing publications and attempts remain intact. [Reading and preservation contract](study-presentation.md).
- New study HTML is approximately 1.07 MB, links the complete original publication and every case attempt, and has keyboard-scrollable tables on narrow screens. An initial offline opening/link check exposed horizontal overflow; it was corrected and the original observation retained. Actual large-study opening, case inspection, full-record retrieval and return focus pass in the final local app. Two earlier single-view main-app observations were approximately 1.3 seconds. The final check took approximately 4.2 seconds while opening the newer reproduction and then navigating to the original edition; it is a different, two-view interaction. None is a p95 or exclusive-load measurement. The per-run exhaustive **Model** reports remain large (up to approximately 186 MB each in this export), because full per-interval service data is repeated in their reading pages. Partitioning those derived reading pages remains required; the smaller study overview does not fix every export.
- Final presentation source is `1e9a2723db55edfdf794ef2033d6ef631ca2757fd9339517d1c4f56d74743b80`. All **100** affected backend/archive/documentation tests pass, including the simultaneous-reader regression and all twelve source-matched teaching topics. Earlier in this checkpoint 67 JavaScript checks and 26 Studies/Model browser checks passed, with three optional browser checks skipped; their exact source scopes are retained. JavaScript and original plant/solar artwork were unchanged by the final Python-only cache and offline-table corrections. The actual full-size study workflow was checked again against the final app. The older 841-test full-suite result remains scoped to source `95f…`; it is not relabelled as a full-suite pass for this source.
- The full original reference bundle is `build/services/study-presentation/reference-study.zip`, SHA-256 `fa5c63bcb9a243192fa4d5a00f893757e79123a31f4eabb357a66b701a6cde18` (847,157,714 bytes). Export took approximately 1,207.33 seconds. A complete temporary restoration checked **608 files and all 36 archives**, including all original computation packets, using the preserved standard-library independent checker. Its 73 local study links resolve, including all 36 case and 36 playback links. The verified duplicate extraction was removed after review; the complete ZIP and check evidence remain. The current presentation source capsule is preserved separately from the original numerical source.
- A separately launched, source-restored reproduction is edition `8430c0fc4a7c49faa6b2fc29d7eda20e`, generated report `75f8aba482f54758991d8197901220f0`, interpreted report **`bc028855ce2f483c9ace33689558255a`**. All 36 cases completed in approximately 470.82 seconds including recording/publication. Every requested/applied action, plant-state vector, numeric diagnostic vector and public work-history digest matches its corresponding parent case exactly. Production, economic totals and ending inventories also match. Predicted trajectories, search metadata and complete archive bytes are not claimed identical.
- **Qualification:** one executed process solve in the reproduction is time-limited: case `fe6495746ce993a02cf7e723`, interval 24, damaged equipment / separate tests / workflow budget / repetition 2. It retains a validated incumbent, no process fallback, and a relative gap of `0.029641950084131227`. The other 1,151 process solves terminate within the requested tolerance. The case's limited-solve count changes from zero to one despite identical executed actions. An initial publication assertion caught that distinction before publishing; the unsuccessful assertion and corrected interpretation are retained. Thus sampled physical repeatability does not certify planning optimality. The original economic and terminal-state qualifications, including the joint-service fallback, still apply. This new edition has all 36 publication-level archive checks; its own full bundle export is not yet claimed.
- Source snapshots, reports, measurements, failed checks/corrections, restored checks and exact artifact identities are in `build/services/study-presentation/verification.json`. Main app session **27162** serves the final source on port 7860 with the same archive `271835a300e75a0a`. All execution, export and test workers from this checkpoint have finished.

Reflection: the complete-size run exposed costs of repeated data that short fixtures missed. This changes the immediate reporting work, while the controller result reinforces the existing distinction between feasibility, repeatability and optimality. Next, partition the remaining large offline Model reading without losing original operands, then continue the broader and terminal-condition studies, remaining service alternatives/belief transitions, deep service essays, all hardware families and held-out learning. **Stages 3–6 and the full six-stage goal remain incomplete; no agreed scope has been reduced.**

### Paged offline Model reading and full preservation

- Added `dispatch-lab/offline-model-pages/1` for new single-run, study and service-example exports. The original essays remain in a reading index; saved parameter/interfaces/evidence, every topic's interval calculations and service work open separately. Long tables continue across labelled pages. Complete calculation JSON, original decision searches and planning snapshots remain available. All output identifies the original recording/execution/documentation separately from the current calculation/reading implementation. Export runs no solver, simulation or teaching example. Missing original explanations and teaching evidence remain missing. [Preservation contract](offline-model-reports.md).
- Final source **`b6c9e714034b90e93f92444f67366b835227944802fce988c1acde79c1fa8bb5`** passes **116** affected backend/archive/documentation tests and all twelve source-matched teaching-evidence topics. Tests cover immutable inputs, exact calculation-interface reconciliation, independently calculated battery energy, complete service records, escaping, bounded pages, original missing-data semantics, shared source capsules and portable checks. Lint, formatting and documentation freshness pass. This is scoped verification, not a relabelled full-suite result. All **29** pre-existing renderer assets are byte-identical to the previous verified presentation capsule; original plant and solar artwork remains unchanged.
- Re-exported the same original computation edition `a3d2bfb295e649b89e93d33354700f7f` and interpreted report `285fcd194f1b486f860063b05b4cbdfa`. The original 32-hour example's reading index is now **87,951 bytes**, versus the earlier exhaustive Model HTML of **185,914,880 bytes**. Its detailed pages stay bounded; full JSON data remains separate. Full export took **649.93 seconds**, versus the previous 1,207.33-second observation. These are single shared-workstation measurements, not p95 or exclusive-load guarantees.
- The complete new ZIP is **955,889,237 bytes**, SHA-256 `133c6613f18c302655d2f06dfb2f320048d089517c3af442d4ae259db076e85a`, at `build/services/model-pages/reference-study.zip`. It is larger in compressed form than the prior bundle because it adds separately preserved calculation data. Expanded size is **5,424,219,311 bytes**. Bounded reading and faster export do not imply smaller compression. The earlier 847 MB ZIP is retained and its original hash was rechecked unchanged.
- A complete temporary restoration verified **58,498 files and all 36 original archives** with the dependency-free checker. All **323,748 local Model links** resolve, original recording/source references match, and all 36 case indexes/detail pages meet the checked size bounds. The original numerical computations, their limitations and the separately reproduced time-limited solve are unchanged. The current rendering source is shared once in the study bundle and remains separate from original numerical source.
- The full-size restored case passes offline Departure Mono, desktop/narrow layout, keyboard table/pagination navigation, original parameters/evidence, calculation/service inspection and existing recorded play/pause/step checks. Its single opening observation was **66.05 ms**; this is not a p95, active-batch or empirical claim. Screenshots were visually reviewed. The agent-led comprehension record distinguishes original/current evidence, default versus run operands, service completion versus confirmation and offline reading versus live learning; it is not participant research.
- A byte-identical, self-contained reading excerpt for the complete chosen case is retained under `build/services/model-pages/reading/`, including its recording and report-source capsule. Only the newly created, fully verified duplicate extraction was removed after review; the full ZIP, all original records and all check evidence remain. Metadata, artifact hashes, measurements and failed/corrected local startup attempt are preserved in `build/services/model-pages/verification.json`.
- Main app session **73580** serves the final source on port 7860 with the same archive `271835a300e75a0a`. The first restricted launch could not bind the port; its connection-refused preview is retained. The authorized localhost launch and repeated playback/battery-learning/evidence/inspector-return preview pass. No physical run was recomputed for this release.

Reflection: large reading pages are now partitioned without changing recorded evidence, and the original preservation/reproduction obligation for the computation reference is met. The programme inventory also confirms that the six baseline sensitivity questions have already run and received interpretations; the former top-level status was stale. Next audit the exact remaining sensitivity export/reproduction obligations, using completed editions and their causal-comparison qualifications, then continue broader terminal/risk studies, post-intervention belief transitions, remaining alternatives and deep service essays. All unfinished hardware families, commissioning, degradation, predictive maintenance and held-out learned/homeostatic training/evaluation remain required. **Stages 3–6 and the full six-stage goal remain active and incomplete.** No finding at this checkpoint requires a scope reduction or user guidance.

### Conditional uncertainty through work and operating tests · 12 September 2026

Checked source **`37c8fcefe5d1010a976320dcbbaf96e6a92ad649027142d2a99f36747ef058d2`** adds opt-in `observed-service-investigation/3` and `observed-recovery-belief/1`. [Mechanics and boundaries](recovery-belief.md); [interpreted examples](../build/services/recovery-belief/write-up.html).

- An observed work-completion descriptor contains only order, action and completion time. It propagates declared reset/replacement probabilities without revealing physical success. Eligible contact packets and informative resource-feasible power tests update the belief. Delayed packets retain their original timing; repeated packets cannot multiply evidence. A separate stuck contact can persist after module replacement. Log probabilities preserve support when a displayed weight underflows. Unknown interventions and unsupported observations leave probabilities unavailable. The point impaired-capacity estimate and static incident hypotheses are explicitly bounded assumptions.
- Version 3 adds an impairment-probability gate after the existing repeated qualified shortfalls. It cannot upgrade operating capacity, skip complete work/return prerequisites, override resources or confirm recovery. Historical follow-up gates retain their decision hour, preceding order and belief identity. Version-1/2 behaviour and the version-2 setup default remain available. Advanced setup and the existing service disclosure reveal the new controls/results; complete histories stay in the recorded calculation rather than the playback summary.
- The standalone standard-library companion independently reconstructs reader quadrature, clipped Gaussian likelihoods, procedure transitions and Bayes updates. Run checks bind those calculations to original public work, observations, per-controller policy and recorded estimates. Admitted tests are also checked with the independent hourly physical reference. Explicit inactive fields omitted by the versioned archive encoding are handled as that encoding, not guessed historical provenance. New bundles include the companion; older captured numerical sources remain unchanged.
- Verification passes **103 affected backend tests**, **23 learning tests**, all twelve source-matched teaching topics, seven changed-renderer Node checks, the investigation-control browser workflow and ten platform preservation checks. Two optional browser fixtures were skipped; the actual new offline reading and live app were separately exercised. Two test-app localhost-readiness failures remain recorded. A diagnostic launch allowed 30 seconds and received a valid response after 6.2013 seconds. The production allowance remains ten seconds. These are scoped checks, not a new full-suite or p95 claim. Twenty-eight of the previous 29 renderer assets are byte-identical; only the revealed service text renderer changed. Original plant/solar artwork, layout, motion and font assets remain unchanged.
- Six 32-hour, seed-7 examples are saved under `build/services/recovery-belief/recorded/37c8fcefe5d1010a976320dcbbaf96e6a92ad649027142d2a99f36747ef058d2/`. All complete their execution window and pass **32,715** independent checks. Every complete bundle was restored and checked with dependencies and network access disabled. Retained restorations provide complete offline Model links. Desktop/narrow reports and the actual probability disclosure were visually reviewed; keyboard return preserves the playhead and focus.
- Resettable-trip and damaged-equipment cases confirm at H19 and H29, producing 250.909091 and 230.909091 kg methane. Uncertain procedures, noisy observations, an unreadable contact and limited power remain unconfirmed and escalate. The power-limited case ends with conditional restoration probability one but an operating estimate of 360 kW: a model probability does not authorise an untested capacity upgrade. Conditions/reliabilities differ across these fixtures, so their production totals are not an isolated policy ranking.
- All six networking-disabled original-source numerical reruns pass and match applied actions, total methane and allocated cost. Saved requested actions, physical states, post-interval diagnostic states, conditional belief records, historical gates and mission-event lists also match exactly across all 192 intervals. Complete solver searches, archive bytes, empirical calibration and deterministic repeatability outside this sample are not claimed. Original and reproduced archives are retained separately.
- The restored-check wrapper initially expected an incorrect return-field name after the independent checker had passed. The example writer also retained successive manifests as immutable siblings; a complete six-case manifest now points to all preserved records. Both workflow corrections and earlier artifacts remain in the evidence. Original documentation snapshots keep the evidence actually captured at their creation. Later verification is not relabelled as original evidence.
- The main app now serves the checked source on port 7860, session **44001**, PID **47276**, with the same original archive `runs/methane-v2/271835a300e75a0a.json.gz`. Its post-restart preview verifies playback, current Model learning/evidence, inspector return and the new opt-in controls. The temporary test app was stopped. Detailed scopes, source identities, hashes and failures are in `build/services/recovery-belief/verification.json`.

Reflection: the agent-led comprehension walkthrough distinguished an attempted procedure, a conditional probability, a historical follow-up decision and operating confirmation. It also distinguished unconfirmed execution from a failed simulation and sampled numerical repeatability from calibrated reliability. This is not participant research. The results support the planned bounded uncertainty approach; no material finding requires user guidance or reduced scope. Do not repeatedly revalidate this frozen checkpoint without a new change or unresolved concern.

The original-source sensitivity queue continues independently in `build/services/sensitivity-completion/`. Recovery reproduction `fdadbcf49fff4e18841a4c9aa0a6a9d1` and cleaning reproduction `fedd20db32574d18907db8ec4309b0c4` each complete all 144 cases, matching original applied actions, reported outcomes, ending plant inventories and recorded event summaries. Their interpreted publications are `18ce6d7f58604cf8b1c6c5a775161204` and `9839d5a9931c4d7481310a90517cfed9`; each publication verifies all 144 original archive references. These are unchanged-seed repetitions of source `c24c…`, not new environmental samples or corrections to the parents' causal-comparison limitations. Remaining queue entries must be audited from their live handles/receipts rather than relaunched blindly. Further original sensitivity exports and reproduction obligations remain open.

**Stages 3–6 and the full six-stage goal remain active and incomplete.** Continue the remaining preservation work alongside investigation-selection alternatives, full service Model examples and broader terminal/risk studies. All unfinished hardware families, commissioning, degradation, predictive maintenance and held-out learned/homeostatic training/evaluation remain required.

### Original investigation alternatives and cold-load investigation · 12 September 2026

- Added `recorded-investigation-alternatives/1` over the saved pre-selection snapshot, catalogue, conditional belief, original process state/forecast and dispatch prices. The revealed service comparison offers inspect-first versus direct intervention at a resolved original choice. Later hours navigate back before calculating. Both branches are recomputed with current code; original predictions remain separate. Expected costs/inventories are labelled statistics, all positive-probability branches must be feasible, and the original restoration requirement remains visible. Actual operating confirmation is never inferred from a predicted procedure outcome. [Interface and boundaries](investigation-alternatives.md).
- Thirty-nine adapter/transport/planning checks and 65 learning/investigator/joint-continuation checks pass. All seven browser comparison tests pass, including incomplete results, branch selection, original-hour navigation, narrow access, cancellation and late-response exclusion. Three renderer checks pass. A separate source-bound teaching run passes all 23 tests at source `cf0d9801b836440df5a3d3640e703ef787a010853feda154d585620b6c426030`; the later UI loading correction has a different source identity and its final evidence refresh remains pending.
- Saved an actual H6 comparison from original run `e8a29c9a4307cf69` in `build/services/investigation-alternatives/recorded/cf0d9801b836440df5a3d3640e703ef787a010853feda154d585620b6c426030/`. Input packet, original/new branch predictions, source capsule and a self-contained HTML report are retained. A separate networking-disabled numerical repeat from the captured source matches reported methane, total decision cost and assumed contribution for both arms. No solver-search or bitwise trajectory identity claim is made. The report passes offline/keyboard/narrow checks and its desktop screenshot was reviewed.
- The actual live-archive walkthrough exposed a pre-existing Gradio HTML constructor/event-name collision. An unset `inputs` attribute was synthesised as a bound custom event because that word appears quoted in JS. Root-response configuration copying consequently traversed the entire Blocks/archived-state graph. A 164.5 MB decoded archive caused repeated 10- and 30-second readiness failures. Local source inspection, a config type census, cold-build profiling and the complete request stack identify the path. The scene now sets its non-reactive `inputs` attribute explicitly to `None`; 11 focused configuration/startup checks pass. The numerical model and original artwork are unchanged. Final live-archive verification, final source-bound UI/evidence capture and main-app restart remain pending at this work-record point. The separate expensive State initialization/eager-export costs are not claimed fixed.
- One diagnostic Python traceback profiler became stuck in its own shutdown after the app had already failed readiness; its known process was terminated. Earlier test attempts/logs and original archives remain retained. No validation bypass, public sharing or change to the installed Gradio package was introduced.
- Information sensitivity repetition `974c865d1ce440489ee415bda6cb7997` is published as `f02d0c420b564a1aad3adcf143dc4f69` after checking all 108 archives. Corrected-support repetition `df24fcd3a621402e9519ceced06b64b9` is published as `8109383253704fedbab9757dce90ea8f` after all 126 cases complete and publication checks pass. All compared applied traces, reported outcomes and event summaries match their respective original editions, with original input/source identities retained. These are computation repetitions, not new environmental evidence or blanket claims that old causal qualifications have been corrected. The existing sequential driver is now running provision sensitivity; interface remains queued. Remaining complete portable-export obligations are still open.
- The all-six-stage goal remains active. This is not completion of stage 4 or the programme. Complete interactive service Model essays, broader terminal/risk evidence, outstanding hardware families/commissioning, degradation and held-out learned/homeostatic policies remain required. No finding currently changes the agreed direction or requires user guidance.

### Bounded planning verification complete; user-requested stop · 12 September 2026

The user requested: **“Finish verifying the bounded planning example and then pause your goal.”** No further feature, study or programme increment is authorised by the earlier continuation instruction until the user resumes work.

- The final actual archived-run walkthrough passes at source `76a41fc80ebe5e12f52b4a7c26d3b88a13825e065c9d9e5db47e84f83aaaecfd`: H19 navigates to the original H6 choice, both alternatives calculate in the isolated worker, individual branches and thermal predictions can be explored, narrow access works, and Escape restores focus while retaining the chosen playhead. Original versus current implementation and conditional versus operating evidence remain explicit. Desktop, branch and narrow screenshots were visually reviewed.
- A legitimate live result with a time-limited baseline and no incumbent is separately preserved with its exact original packet and current source capsule. The completed sample does not erase that result. The standalone saved report passes offline reading and visual review; its separate captured-source numerical repeat retains zero differences in both arms' reported methane, total decision costs and assumed contribution. These are bounded predictions and sampled numerical repetitions, not a claim of global optimality, calibrated reliability or universally successful finite-time solves.
- Final source-bound teaching checks pass all 23 tests and all twelve topics. The 39 adapter/transport/planning checks, 65 learning/investigation/continuation checks, eleven configuration/startup checks, seven browser workflows and three renderer checks retain their individual source scopes. Repeated teaching checks are not counted as additional distinct cases. Documentation freshness passes. No full-suite or p95 claim is made. Existing plant/solar SVG, styles, motion and font assets remain byte-identical; only alternative-rendering/wiring JavaScript differs from the previous checkpoint.
- The normal local app is restarted at port 7860, session **98538**, PID **57639**, with the unchanged selected archive `runs/methane-v2/271835a300e75a0a.json.gz`. Playback, current learning/evidence and inspector/focus return pass after restart. The temporary recorded-example server is stopped. Earlier readiness failures and the independently identified Gradio configuration fix remain in the record; separate cold State/eager-export cost is not claimed fixed.
- The verified background queue launcher (PID 37328) was stopped before cooperative cancellation of its active provision worker. Edition `685e25a6475c434786f7c9cf66d9ff0d` has **29 complete and 79 cancelled/unstarted cases**, retains its original attempt and automatically generated incomplete publication `2f405d09ef5344bfb393469e717726bd`, and has no active worker. Interface sensitivity has not started. `build/services/sensitivity-completion/user-pause-confirmed.json` preserves the stop receipt; the provision pointer records explicit same-edition resume instructions. Original completed publications and archives remain intact. No further numerical study, interpretation or export was started.

[Readable verification](../build/services/investigation-alternatives/verification.md) and [detailed evidence](../build/services/investigation-alternatives/verification.json) capture the checkpoint. Work is stopped at user request; the remaining stage-3 preservation, stage-4 documentation/terminal-risk work and all unfinished hardware/degradation/learning obligations remain recorded. This stop does not mark the six-stage objective complete or blocked.

Pause-control limitation: programme work and the numerical queue are stopped, but the app-level goal status cannot be paused through the exposed goal tools. The attempt to access Codex's own Pause control was rejected by Computer Use: “Computer Use is not allowed to use the app 'com.openai.codex' for safety reasons.” No workaround or false Complete/Blocked status was applied. The user was informed to use the goal's Pause control. Any automatic continuation must respect the user's stop instruction and must not resume implementation or studies.

### Realism requirement recorded; implementation remains stopped · 12 September 2026

The user required a realism pass over the first complete service system, the plant assumptions that determine service value, and initial feasibility of all 14 hardware families. Added `docs/field-operations-realism.md` and a roadmap gate. The register must separate capability evidence from uncertain numerical inputs, identify operating/support prerequisites and consequential gaps, and bind evidence to the exact model/task. Structural capability and information boundaries are reviewed before parameter sensitivity. Independent numerical verification remains distinct from empirical support; sensitivity studies must report reversals and unanswered questions, not only stable rankings.

Stage 1/2 status wording now explicitly says implementation/numerical completion with the realism gate open. No scientific review was claimed complete, no assumptions or capability settings changed, no experiments or learned-policy work started, and no old evidence was rewritten. Local document links and requested scope were checked. The next substantive work, when the user resumes the programme, is the realism review. The goal remains blocked awaiting resumption, not complete.
